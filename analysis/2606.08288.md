---
analysis_of: 2606.08288
title: "MotionVLA: Injecting Geometric Motion into Vision-Language-Action Model"
alias: MotionVLA
tagline: 과거 헤드캠 열 프레임을 얼린 궤적장 추출기로 토큰화해, 현재 프레임이 교차어텐션으로 꺼내 쓰는 과거 전용 움직임 기억을 π0 에 붙인다
authors: Shanglin Yuan, Weiheng Zhao, Xianda Guo et al. (Huazhong University of Science and Technology · D-Robotics · Wuhan University)
pillars: P2, P4, P3
tags: [vla-arch, memory, history, 4d, trajectory-field, cross-attention, motion-tokens, pi0, flow-matching, auxiliary-supervision, observation-design, path-efficiency, libero, robotwin, real-world]
links: [arxiv|https://arxiv.org/abs/2606.08288]
published: 2026-06-06
generated: 2026-09-09 12:35
generator: analyze/v3
arxiv_html: 2606.08288v1
arxiv_fetched: 2026-09-09
figures: [S1.F1, S2.F2, A1.F3, A1.F4, A2.F5, A3.F6, A3.F7, A3.F8, A3.F9]
appendix: [A.1, A.2, A.3, A.4, B, C]
terms: 16
metric: RoboTwin2.0 평균 41% → 53%
summary: >
  같은 장면이 "계속할 차례"일 수도 "다음으로 넘어갈 차례"일 수도 있어서, 현재 한 장만 보는 VLA 는
  긴 과제에서 길을 잃습니다. 그런데 이 논문의 측정에서 **과거 프레임을 그대로 붙이면 37% 가 13%** 로
  무너지고, 프레임마다 따로 3D 로 띄운 4D 토큰은 성공하고도 **전문가보다 1.36 배 긴 경로**를 돌아갑니다.
  저자들은 과거 열 프레임을 얼린 궤적장 추출기로 **시간 연속 움직임 토큰**으로 접고, 현재 프레임이
  교차어텐션으로 그 기억을 **묻게** 한 뒤 과거 행동을 복원시켜 접지합니다. RoboTwin2.0 평균은
  **41% 에서 53%**, LIBERO-Long 은 **85.2 에서 91.2**, 경로 비율은 **1.05** 까지 내려옵니다.
---

# 과거를 프레임으로 기억하면 37 이 13 으로 무너지고, 프레임 사이의 움직임으로 기억하면 54 가 된다

## 1 무엇이 문제인가

### 같은 장면이 두 가지 제어 상태를 뜻할 때 | Visual Aliasing · State Chaos · Reactive Policy

긴 조작 과제에서 정책이 알아야 하는 것은 지금 무엇이 보이느냐만이 아니라 **어떻게 여기까지
왔느냐**입니다. 세 블록을 순서대로 건드리는 과제를 떠올리면, 집게가 빨간 블록 위에 떠 있는 장면은
"지금 막 닿으려는 중"일 수도 있고 "이미 닿았으니 초록으로 갈 차례"일 수도 있습니다. 두 상태의 관찰이
같으므로, 현재 한 장에서 행동을 내는 [반응형 정책](term:reactive)은 어느 쪽인지 정할 근거가 없습니다.
저자들은 이 현상을 [시각 앨리어싱](term:aliasing)이라 부르고, 그것이 정책을 같은 서브골 안에서
맴돌게 하거나 단계를 건너뛰게 하는 [state chaos](term:state-chaos) 를 부른다고 씁니다.

```probe-term
{"id": "reactive", "title": "반응형 정책 (reactive policy)",
 "body": "현재 관찰 한 장(과 고유수용 상태, 언어 지시)만으로 다음 행동을 내는 정책. 대부분의 VLA 가 이 형태이고, 이 논문이 문제 삼는 것은 그 구조가 '어떻게 여기까지 왔는가'를 판단에 넣을 길이 없다는 점입니다."}
```

```probe-term
{"id": "aliasing", "title": "시각 앨리어싱 (visual aliasing)",
 "body": "서로 다른 제어 상태가 **같은 관찰**로 나타나는 것. 다단계 과제에서 '이 서브골을 계속할지, 끝낼지, 다음으로 갈지'가 최근 움직임에 따라 갈리는데 현재 프레임은 그 셋을 구분하지 못합니다. 이 논문의 문제 설정 전체가 이 한 단어에서 출발합니다."}
```

```probe-term
{"id": "state-chaos", "title": "state chaos",
 "body": "시각 앨리어싱 때문에 정책이 과제 단계를 혼동해 같은 서브골을 반복하거나 건너뛰는 실패 양상. 저자들은 MemoryVLA·4D-VLA·SwiftVLA 를 인용하며 선행 연구가 이미 이름 붙인 현상으로 소개합니다."}
```

여기까지는 이력을 넣자는 논문들이 공통으로 깔고 시작하는 전제입니다. 이 논문이 한 걸음 더 나가는
지점은 **기억을 문맥이 아니라 제어 인터페이스로** 본다는 데 있습니다. 과거가 정책에 얼마나 많이
주어지느냐가 아니라, 그 과거가 어떤 구조로 주어지느냐가 행동 생성이 안정적으로 그것을 쓸 수 있는지를
결정한다는 주장입니다. 이 문장은 뒤의 절제 실험에서 숫자로 돌아옵니다.

바탕이 되는 정책은 [π0](term:pi0) 입니다. 다시점 RGB 관찰과 고유수용 상태, 언어 지시를 토큰으로
잇고, 언어 모델 백본이 낸 프리픽스를 조건으로 [플로우 매칭 행동 전문가](term:flow-expert)가
$`H`$ 스텝짜리 행동 청크 $`\mathbf{a}_t\in\mathbb{R}^{H\times d_a}`$ 를 노이즈에서 적분해 냅니다.
이 논문은 이 구조의 어느 것도 바꾸지 않고, 프리픽스에 들어가는 **시각 토큰만** 손댑니다.

```probe-term
{"id": "pi0", "title": "π0",
 "body": "PaliGemma-3B 위에 플로우 매칭 행동 전문가를 붙인 VLA. 다시점 이미지·언어·상태를 토큰으로 이어 언어 모델에 넣고, 그 출력을 조건으로 연속 행동 청크를 생성합니다. 이 논문이 개조하는 대상이자 모든 절제 실험의 기준선입니다."}
```

```probe-term
{"id": "flow-expert", "title": "플로우 매칭 행동 전문가",
 "body": "노이즈 행동 a^τ 와 시각 τ∈[0,1] 를 받아 속도장 v_θ 를 예측하고, 그것을 τ=0 에서 1 까지 적분해 행동 청크를 만드는 디코더. 학습은 보간된 노이즈 행동에서 (a−ε) 를 회귀하는 행동 복제이고, 이 논문은 그 손실을 그대로 둔 채 보조 손실만 하나 더합니다."}
```

> [!NOTE] 이 논문의 질문은 "이력을 줄 것인가"가 아니다
> 저자들이 초록에 그대로 적은 물음은 이렇습니다 — VLA 는 **과거 프레임들**을 기억해야 하는가,
> **프레임들을 잇는 움직임**을 기억해야 하는가. 이력의 양이 아니라 이력의 형태를 변수로 놓은 것이고,
> 그래서 이 논문의 대조군은 "이력 없음"만이 아니라 "이력을 프레임으로 줌", "이력을 프레임별 4D 로 줌"
> 까지 셋입니다.

```probe-quiz
{"q": "이 논문이 문제로 삼는 '시각 앨리어싱'은 무엇인가?",
 "options": ["카메라 해상도가 낮아 작은 물체가 구분되지 않는 것",
             "서로 다른 제어 상태(계속·종료·다음 단계)가 같은 현재 관찰로 나타나 한 장만으로는 갈라지지 않는 것",
             "다시점 카메라 사이의 보정 오차로 같은 점이 다른 위치에 맺히는 것"],
 "answer": 1,
 "why": "논문의 정의는 '같은 현재 관찰이 최근 움직임에 따라 다른 제어 상태에 대응한다'는 것이고, 그래서 처방이 이력입니다. 해상도는 논문에 등장하지 않고, 다시점 보정 오차는 §1 이 '프레임별 3D 띄우기의 기하 불일치'로 다루는 별개 문제입니다."}
```

### 4D 를 더 넣었는데 경로는 더 돌아갔다 | Discrete 4D Injection · Geometric Drift · Path Efficiency

최근 VLA 들은 이 앨리어싱을 풀려고 과거를 여러 형태로 넣습니다 — 프레임 스태킹, 메모리 모듈, 명시적
3D 기하, 그리고 시간과 공간을 함께 주는 [4D 주입](term:4d-injection). 저자들의 관찰은 그 흐름에
깔린 가정 하나를 겨눕니다. **주입된 증거는 그 자체로 제어에 쓸 만큼 정합적이어야 한다**는 가정인데,
대표적 4D 파이프라인에서는 그것이 성립하지 않는다는 것입니다.

```probe-term
{"id": "4d-injection", "title": "이산 4D 주입 (discrete 4D injection)",
 "body": "과거 프레임 몇 장을 고르고, 각 프레임을 RGB-D 역투영이나 학습된 기하 예측기로 **따로** 3D 토큰으로 띄운 뒤, 메모리 뱅크나 키프레임 샘플링으로 솎아 정책 프리픽스에 넣는 패턴. 4D-VLA 와 SwiftVLA 가 이 계열이고, 이 논문이 '형태는 4D 지만 시간적으로 정합적이지 않다'고 지목하는 대상입니다."}
```

증상은 성공률이 아니라 **경로**에서 보입니다. 저자들은 [경로 효율](term:pe)이라는 지표를 세웁니다 —
정책이 실행한 말단 이동 거리를 전문가 시연의 이동 거리로 나눈 비율로, 1 이면 시연만큼 곧게 갔다는
뜻입니다. LIBERO 에서 4D-VLA 는 과제를 끝내면서도 전문가보다 1.36 배(Goal)·1.32 배(Long)를 돌아갔고,
깊이를 실측 대신 추정으로 넣은 변형은 1.88 배·2.06 배까지 벌어집니다. π0 자체도 1.23·1.19 로 이미
곧지 않은데, 4D 를 더한 쪽이 더 돌아간 것입니다.

```probe-term
{"id": "pe", "title": "경로 효율 (path efficiency, PE)",
 "body": "에피소드 동안 말단 위치의 인접 스텝 거리를 모두 더한 이동 길이를 정책과 전문가 시연에서 각각 구하고, 그 비율 L_policy / L_expert 를 취한 값. 1 이 전문가와 같은 길이이고 클수록 우회가 많습니다. 성공한 에피소드만 평균하므로 항상 성공률과 함께 읽어야 한다고 논문이 못 박습니다."}
```

```probe-scale
{"title": "LIBERO-Goal 경로 효율 — 전문가 대비 몇 배를 돌아가는가",
 "rows": [
   {"label": "MotionVLA", "n": 1.05, "value": "1.05", "us": true},
   {"label": "π0 (베이스라인)", "n": 1.23, "value": "1.23"},
   {"label": "4D-VLA", "n": 1.36, "value": "1.36"},
   {"label": "4D-VLA* (추정 깊이)", "n": 1.88, "value": "1.88"}]}
```

왜 그렇게 되는지에 대한 저자들의 진단은 두 갈래입니다. 첫째, **기하 불일치** — 프레임을 따로 띄우면
같은 물리적 점이 프레임마다 다른 3D 위치로 흘러갑니다. 둘째, **시간 파편화** — 희소하거나 불균등한
키프레임 샘플링은 움직임 증거를 불규칙하게 만들어 정책이 쓰기 어렵게 합니다. 이 둘이 행동 생성으로
번지면 떨리는 보정, 불안정한 최적화, 우회 실행이 됩니다.

```probe-figure
{"id": "S1.F1", "url": "https://arxiv.org/html/2606.08288v1/intro-corl.png",
 "caption": "이산 4D 증거는 파편화될 수 있는 반면, 궤적장은 더 정합적인 움직임 인터페이스를 제공합니다. 이력을 따로 띄운 프레임의 희소 집합으로 다루는 대신, 최근 관찰을 질의 가능한 시간 연속 움직임 증거로 표현해 더 매끄럽고 곧은 제어를 노립니다.",
 "source": "Figure 1, 원문 §1"}
```

이 논문이 서는 계보는 "과거를 어떤 형태로 넣는가"를 두고 한 해 반 동안 갈라져 온 줄입니다. 화면에
그리는 쪽, 프레임별 3D 로 띄우는 쪽, 잠재 움직임으로 접는 쪽이 있었고, 이 논문은 그중 어느 것도
아닌 **시간 연속 3D 궤적장**을 고릅니다.

```probe-lineage
{"title": "VLA 에 과거를 넣는 계보 — 픽셀 위 궤적에서 시간 연속 궤적장으로",
 "items": [
   {"when": "2024-12", "what": "TraceVLA — 점 추적 궤적을 관찰 이미지 위에 그려 넣음",
    "note": "과거를 2D 픽셀 경로로 렌더링해 같은 이미지 채널에 넣습니다. 이 논문은 이를 '렌더링된 궤적으로 이력을 압축하는' 계열로 분류합니다.",
    "link": "https://arxiv.org/abs/2412.10345", "link_label": "arXiv:2412.10345"},
   {"when": "2025-02", "what": "ARM4R — 2D 추적을 3D 궤적으로 띄워 로봇 사전학습",
    "note": "궤적을 3D 로 올린 첫 줄이지만 목적이 사전학습 표현이지 정책의 온라인 기억은 아닙니다.",
    "link": "https://arxiv.org/abs/2502.13142", "link_label": "arXiv:2502.13142"},
   {"when": "2025-06", "what": "CronusVLA — 잠재 움직임을 시간 축으로 옮겨 다중 프레임 예측",
    "note": "이력을 압축된 시간 청크로 다룹니다. 이 논문의 분류로는 '컴팩트 시간 청크' 계열입니다.",
    "link": "https://arxiv.org/abs/2506.19816", "link_label": "arXiv:2506.19816"},
   {"when": "2025-06", "what": "4D-VLA — 순차 RGB-D 를 메모리 뱅크 샘플링으로 인코딩",
    "note": "프레임별로 3D 를 띄우고 키프레임을 솎는 이산 4D 주입의 대표. 이 논문의 경로 효율 비교에서 1.36 배를 돌아가는 바로 그 대조군입니다.",
    "link": "https://arxiv.org/abs/2506.22242", "link_label": "arXiv:2506.22242"},
   {"when": "2025-08", "what": "MemoryVLA — 지각·인지 기억을 검색해 쓰는 VLA",
    "note": "기억을 검색하는 발상은 공유하지만, 검색되는 것이 프레임 특징이지 움직임은 아닙니다.",
    "link": "https://arxiv.org/abs/2508.19236", "link_label": "arXiv:2508.19236"},
   {"when": "2025-10", "what": "TraceAnything — 영상의 모든 픽셀을 시간 연속 3D 궤적으로 표현하는 궤적장",
    "note": "이 논문이 얼린 채 그대로 가져다 쓰는 추출기. 프레임별이 아니라 창 전체를 한 번에 궤적 함수로 만듭니다.",
    "link": "https://arxiv.org/abs/2510.13802", "link_label": "arXiv:2510.13802"},
   {"when": "2025-12", "what": "SwiftVLA — 2D 스트림과 시간 캐시에서 보조 4D 특징을 유도",
    "note": "경량 모델에 4D 를 싸게 넣는 쪽. LIBERO 평균 95.1 로 이 논문 바로 아래에 있습니다.",
    "link": "https://arxiv.org/abs/2512.00903", "link_label": "arXiv:2512.00903"},
   {"when": "2026-06", "what": "MotionVLA — 과거 전용 창을 궤적장 토큰으로 접고, 현재 프레임이 교차어텐션으로 검색",
    "current": true,
    "note": "프레임을 저장하지 않고 프레임 사이의 움직임만 저장하며, 그 기억을 현재 관찰이 질의한 뒤 과거 행동 복원으로 접지한다는 세 가지가 앞의 것들과 갈립니다."}]}
```

```probe-quiz
{"q": "저자들이 기존 4D 주입의 실패로 지목한 증상은?",
 "options": ["과제 성공률이 π0 보다 크게 떨어진다",
             "과제는 끝내면서도 말단 경로가 전문가보다 훨씬 길게 돌아간다",
             "추론 속도가 실시간에 못 미친다"],
 "answer": 1,
 "why": "§1 은 '에피소드를 완료하면서도 눈에 띄게 덜 곧은 경로'를 관찰이라고 적고, Table 5 의 경로 효율 1.36·1.88 이 그 근거입니다. 4D-VLA 의 성공률은 π0 보다 낮긴 하지만 논문이 문제 삼는 축은 성공률이 아니며, 추론 속도는 부록 A.2 에서 이 논문 자신의 비용으로만 다뤄집니다."}
```

## 2 무엇을 바꿨나

### 현재 프레임을 빼고, 과거 열 프레임을 궤적장으로 접는다 | Past-only Window · Trajectory Field · TraceAnything · FIFO Buffer

시점 $`t`$ 에서 이 논문은 헤드 카메라 프레임만 씁니다. 그것도 현재 프레임 $`I_t^{\mathrm{head}}`$ 는
**명시적으로 제외**하고, 그 앞의 프레임들에서 보폭 $`\Delta`$ 로 $`T_{\mathrm{hist}}`$ 장을 고릅니다.

```probe-eq
{"read": "시점 t 의 이력 창은 t−1 에서 시작해 보폭 Δ 로 거슬러 올라간 T_hist 장이며, 에피소드 시작 전 시각은 첫 프레임을 반복해 채운다",
 "tex": "\\mathcal{W}_t=\\left[\\tilde{I}^{\\mathrm{head}}_{t-1-(T_{\\mathrm{hist}}-1)\\Delta},\\;\\ldots,\\;\\tilde{I}^{\\mathrm{head}}_{t-1-\\Delta},\\;\\tilde{I}^{\\mathrm{head}}_{t-1}\\right]",
 "symbols": [
   {"sym": "T_{\\mathrm{hist}}", "name": "선택 프레임 수", "note": "기본 5"},
   {"sym": "\\Delta", "name": "시간 보폭", "note": "기본 2 — 창이 덮는 원시 프레임은 약 T_hist·Δ = 10 장"},
   {"sym": "\\tilde{I}", "name": "패딩된 프레임", "note": "τ 가 에피소드 시작 전이면 가장 이른 프레임을 반복"},
   {"sym": "I_t^{\\mathrm{head}}", "name": "현재 헤드캠 프레임", "note": "창에 들어가지 않습니다 — 현재는 질의 쪽에서만 쓰입니다"}]}
```

기본값은 헤드캠 10 FPS 에 $`T_{\mathrm{hist}}=5`$, $`\Delta=2`$ 이니 창은 **직전 1 초의 열 프레임**에서
한 장 걸러 다섯 장입니다. 학습 때는 같은 에피소드를 인덱싱해 창을 만들고, 추론 때는 에피소드별
[FIFO 버퍼](term:fifo)로 똑같은 정의를 온라인으로 구현합니다. 저자들이 강조하는 것은 **학습과 시험이
동일한 이력 인터페이스**를 쓴다는 점입니다 — 이력 부족 시의 왼쪽 패딩까지 같습니다.

```probe-term
{"id": "fifo", "title": "FIFO 버퍼 (per-episode)",
 "body": "추론 때 직전 프레임을 뒤에 넣고 가장 오래된 프레임을 밀어내는 고정 길이 큐. 학습 때 에피소드를 인덱싱해 만든 창과 같은 정의를 온라인에서 재현하는 장치이고, 길이가 1+(T_hist−1)Δ 에 못 미치면 첫 프레임으로 왼쪽을 채웁니다."}
```

이 창을 받는 것이 [궤적장](term:traj-field) 추출기 [TraceAnything](term:traceanything) 입니다. 얼린
채로, 스텝마다 한 번 돕니다. 궤적장은 창의 모든 픽셀에 대해 정규화 시간 $`[0,1]`$ 위의 연속 3D 궤적
함수 $`\mathbf{x}_{i,u,v}(\cdot)`$ 를 주는데, 이 논문은 그 밀집 출력을 쓰지 않고 추출기 디코더의
**마지막 층 토큰** $`\mathbf{Z}=\mathrm{TrajEnc}_{\mathrm{tok}}(\mathbf{I}^{\mathrm{head}}_{1:T})`$ 만
가져옵니다.

```probe-term
{"id": "traj-field", "title": "궤적장 (trajectory field)",
 "body": "입력 프레임 i 의 픽셀 (u,v) 마다 정규화 시간 위의 연속 3D 궤적 함수를 대응시키는 4D 표현. 프레임을 따로 3D 로 띄우는 것과 달리 창 전체가 하나의 시간 연속 함수로 표현되므로, 같은 물리적 점이 프레임마다 흘러 다니는 기하 불일치가 표현 단계에서 생기지 않습니다."}
```

```probe-term
{"id": "traceanything", "title": "TraceAnything",
 "body": "영상을 궤적장으로 표현하는 사전학습 모델. 이 논문은 이것을 **얼린 채** 헤드캠 이력 창에 스텝마다 한 번 돌리고, 밀집 궤적 대신 디코더 마지막 층의 시공간 토큰을 가져옵니다. 가림·빠른 움직임·무질감 장면에서 실패할 수 있다는 것이 저자들이 적은 한계입니다."}
```

Algorithm 1 은 이 절차 전체를 열다섯 줄로 적습니다. 인라인 SVG 라 원문 그림을 가져올 수 없으니
그대로 옮깁니다.

```text Algorithm 1 — 질의 가능한 움직임 기억 M_kv 를 만드는 절차 (원문 §3.2)
Input : step t, history length T_hist, stride Δ
Input : episode frames or FIFO buffer B
 1: if training then
 2:     B ← [I_head_1, ..., I_head_{t-1}]
 3: else
 4:     Append I_head_{t-1} to B and evict the oldest frame
 5: end if
 6: Left-pad B with its earliest frame until |B| ≥ 1 + (T_hist − 1)Δ
 7: L ← |B|
 8: W_t ← StrideSample(B, T_hist, Δ)
 9: W_t ← Norm(Resize(W_t))
10: Z_t ← TrajEnc_tok(W_t)                      # frozen TraceAnything, final decoder tokens
11: M_kv ← PosEnc(W_z Z_t)  ∈ R^{B × S_4D × E}   # project to VLA hidden size, add position
12: return M_kv
```

> [!NOTE] 왜 현재 프레임을 굳이 뺐는가
> 현재 프레임은 이미 VLA 의 시각 인코더를 거쳐 프리픽스에 들어가 있습니다. 이력 창에도 넣으면
> 같은 정보가 두 경로로 들어가고, 기억이 "지금"을 요약하는 쪽으로 기울 여지가 생깁니다. 창을 **과거
> 전용**으로 못 박아야 기억의 역할이 '최근 물리적 진행을 드러내는 것'으로 남고, 뒤에 나오는 검색에서
> 현재가 과거를 **묻는** 구도가 성립합니다.

```probe-quiz
{"q": "기본 설정에서 이력 창이 실제로 덮는 시간은?",
 "options": ["헤드캠 10 FPS 에서 직전 약 1 초 — 원시 프레임 10 장 중 한 장 걸러 5 장",
             "에피소드 시작부터 현재까지 전부",
             "직전 5 초 — 매초 한 장씩 5 장"],
 "answer": 0,
 "why": "§4.1 이 명시합니다: T_hist=5, Δ=2 로 원시 프레임 10 장을 덮고, 스트림은 10 FPS 입니다. 에피소드 전체는 부록 A.2 의 긴 창 실험에서 오히려 성능을 떨어뜨리는 쪽이고, 매초 한 장은 Δ=10 에 해당하는 설정으로 논문에 없습니다."}
```

### 밀집 궤적장이 아니라 디코더 토큰만 남긴다 | Compact Token Interface · Key-Value Memory · Projection W_z

추출기가 내놓는 밀집 궤적장을 VLA 에 그대로 먹이지 않는 선택은 두 가지 이유에서입니다. 픽셀마다
궤적이니 **토큰이 폭발**하고, 국소 추적 노이즈에 **민감**해집니다. 대신 마지막 디코더 토큰
$`\mathbf{Z}_t`$ 를 선형 사영 $`W_z`$ 로 VLA 은닉 크기 $`E`$ 에 맞추고 위치 부호화를 더해
$`\mathbf{M}_{\mathrm{kv}}\in\mathbb{R}^{B\times S_{\mathrm{4D}}\times E}`$ 를 만듭니다.

```probe-split
{"cards": [
  {"title": "밀집 궤적장 출력", "tag": "픽셀마다 궤적",
   "body": "창의 모든 픽셀에 대한 연속 3D 궤적 함수. 정보는 가장 많지만 토큰 수가 해상도에 비례해 터지고, 국소 추적 실패가 그대로 입력 노이즈가 됩니다.",
   "note": "이 논문이 쓰지 않기로 한 쪽"},
  {"title": "디코더 마지막 층 토큰", "tag": "S_4D 개", "us": true,
   "body": "궤적장을 만들기 직전의 시공간 토큰. 프레임 사이 움직임 증거는 담고 있으면서 길이가 고정돼 있고, W_z 하나로 VLA 차원에 맞춥니다.",
   "note": "키-값 기억으로만 쓰이고 프리픽스에 직접 들어가지 않습니다"}]}
```

결정적인 것은 마지막 문장입니다. $`\mathbf{M}_{\mathrm{kv}}`$ 는 프리픽스에 이어 붙는 토큰이
**아니라**, 뒤의 검색에서 키와 값으로만 쓰이는 기억입니다. 무엇을 읽을지는 여전히 현재 관찰이
정합니다 — 이것이 "이력을 넣되 문맥을 늘리지 않는" 이 논문의 방식입니다.

```probe-quiz
{"q": "M_kv 가 프리픽스에 직접 들어가지 않는다는 것의 의미는?",
 "options": ["이력 정보가 백본에 전혀 도달하지 않는다",
             "이력은 키-값 기억으로만 존재하고, 현재 시각 토큰이 어텐션으로 읽어 낸 결과만 프리픽스에 들어간다",
             "이력 토큰이 텍스트 토큰 뒤에 붙는다"],
 "answer": 1,
 "why": "§3.2 는 M_kv 가 '오직 나중 검색을 위한 키-값 기억'이라고 적고, §3.3 의 교차어텐션 출력 M 이 다시 다시점 토큰과 섞여 백본에 들어갑니다. 이력이 도달하지 않는 것도, 텍스트 뒤에 붙는 것도 아닙니다."}
```

### 현재가 과거를 묻는다 — Decouple | Query-conditioned Retrieval · Cross-Attention · Motion-conditioned Tokens

방법의 이름인 Decouple-then-Recouple 은 네 단계 사슬입니다. (i) 궤적 추출기로 4D 증거를 뽑고,
(ii) 질의 조건 검색으로 과제 관련 움직임을 **떼어 내고**, (iii) 검색된 토큰이 시간 접지를 잃지 않게
감독하고, (iv) 그것을 현재 지각과 **다시 합쳐** 행동을 냅니다.

```probe-figure
{"id": "S2.F2", "url": "https://arxiv.org/html/2606.08288v1/overview_corl.png",
 "caption": "MotionVLA 개요. 궤적장 토큰으로 과거 전용 움직임 기억을 만들고, 현재 시각 특징이 교차어텐션으로 그 기억을 질의해 과제 관련 움직임 토큰을 검색한 뒤 VLA 스트림에 다시 합칩니다. 오른쪽 위의 보조 궤적 복원 헤드가 검색된 토큰을 제어 관련 동역학에 접지시킵니다.",
 "source": "Figure 2, 원문 §3.3"}
```

떼어 내는 단계는 [다중 헤드 교차어텐션](term:mha) 한 층입니다. VLA 시각 인코더가 낸 현재 헤드캠
토큰 $`\mathbf{V}^{\mathrm{head}}\in\mathbb{R}^{B\times S_{\mathrm{img}}\times E}`$ 가 질의가 되고,
층 정규화를 거친 $`\mathbf{M}_{\mathrm{kv}}`$ 가 키와 값이 됩니다.

```probe-eq
{"read": "현재 헤드캠 시각 토큰을 질의로, 층 정규화한 움직임 기억을 키와 값으로 교차어텐션을 돌려 움직임 조건 토큰 M 을 얻는다",
 "tex": "\\mathbf{M}=\\mathrm{MHA}\\!\\left(\\mathbf{V}^{\\mathrm{head}},\\;\\mathrm{LN}(\\mathbf{M}_{\\mathrm{kv}}),\\;\\mathrm{LN}(\\mathbf{M}_{\\mathrm{kv}})\\right)\\in\\mathbb{R}^{B\\times S_{\\mathrm{img}}\\times E}",
 "symbols": [
   {"sym": "\\mathbf{V}^{\\mathrm{head}}", "name": "현재 헤드캠 시각 토큰", "note": "VLA 자신의 시각 인코더 출력. 질의 자리"},
   {"sym": "\\mathbf{M}_{\\mathrm{kv}}", "name": "움직임 기억", "note": "Algorithm 1 의 출력. 키와 값 자리, LN 을 거침"},
   {"sym": "\\mathbf{M}", "name": "움직임 조건 토큰", "note": "길이는 현재 헤드캠 토큰과 같은 S_img — 문맥이 길어지지 않습니다"},
   {"sym": "S_{\\mathrm{4D}}", "name": "기억 토큰 수", "note": "출력 길이에 영향을 주지 않습니다"}]}
```

```probe-term
{"id": "mha", "title": "다중 헤드 교차어텐션 (MHA, query ≠ key/value)",
 "body": "질의 집합이 다른 집합(키·값)을 읽어 오는 어텐션. 여기서는 현재 프레임 토큰 하나하나가 과거 움직임 기억 전체를 훑어 자기와 관련된 부분을 가중합으로 가져옵니다. 출력 길이가 질의 길이와 같다는 성질이 '문맥을 늘리지 않는 이력'을 만드는 핵심입니다."}
```

출력 $`\mathbf{M}`$ 은 토큰 수가 현재 헤드캠 토큰과 **정확히 같습니다**. 이력이 열 프레임이든 스무
프레임이든 프리픽스 길이는 변하지 않고, 각 토큰이 더 긴 과거를 조건으로 삼을 뿐입니다. 저자들은 이것을
"모든 이력 특징을 프리픽스에 순진하게 이어 붙이는 것"의 반대편에 놓습니다 — 과거를 현재 관찰에
**정렬**하는 것이라고요.

```probe-quiz
{"q": "교차어텐션 출력 M 의 토큰 길이는 무엇과 같은가?",
 "options": ["움직임 기억 M_kv 의 토큰 수 S_4D",
             "현재 헤드캠 시각 토큰 수 S_img",
             "S_img 와 S_4D 의 합"],
 "answer": 1,
 "why": "식 (2) 아래 문장이 명시합니다 — M 은 현재 헤드뷰 질의와 같은 길이이고, 각 토큰이 더 긴 과거 전용 기억을 조건으로 삼습니다. 합이 되는 것은 이력을 프리픽스에 이어 붙이는 방식이고, 이 논문이 피하려는 쪽입니다."}
```

### 검색된 토큰이 겉모습 요약으로 무너지지 않게 — 과거 행동을 복원시킨다 | Trajectory-grounded Supervision · Auxiliary Loss · History-action Reconstruction

교차어텐션만으로는 $`\mathbf{M}`$ 이 **겉모습 요약**으로 무너질 수 있습니다 — 움직임이 아니라 프레임
사이의 정적 차이만 담는 쪽으로 학습이 흘러도 행동 손실은 그것을 막지 않습니다. 그래서 학습 목적은
두 항입니다: $`\mathcal{L}=\mathcal{L}_{\mathrm{action}}+\alpha\,\mathcal{L}_{\mathrm{traj}}`$.

행동 항은 π0 의 플로우 매칭 행동 복제 그대로입니다.

```probe-eq
{"read": "노이즈와 전문가 행동을 τ 로 보간한 점에서 정책이 예측한 속도장이 목표 속도 (a−ε) 에 가깝도록 L2 로 회귀한다",
 "tex": "\\mathcal{L}_{\\mathrm{action}}=\\mathbb{E}\\!\\left[\\left\\|\\mathbf{v}_{\\theta}(\\mathbf{a}^{\\tau},\\tau;\\text{prefix})-(\\mathbf{a}-\\boldsymbol{\\epsilon})\\right\\|_{2}^{2}\\right],\\qquad \\mathbf{a}^{\\tau}=(1-\\tau)\\boldsymbol{\\epsilon}+\\tau\\mathbf{a}",
 "symbols": [
   {"sym": "\\mathbf{a}", "name": "전문가 행동 청크", "note": "H × d_a"},
   {"sym": "\\boldsymbol{\\epsilon}", "name": "가우시안 노이즈", "note": "𝒩(0, I) 에서 표본"},
   {"sym": "\\tau", "name": "플로우 시각", "note": "𝒰(0,1) 에서 표본"},
   {"sym": "\\text{prefix}", "name": "백본 프리픽스", "note": "이 논문에서는 재결합된 토큰 F 를 포함"}]}
```

접지 항이 새 것입니다. 이력 창과 **같은 구간에 정렬된 행동열** $`\mathbf{a}_{\mathrm{hist}}\in
\mathbb{R}^{T_{\mathrm{hist}}\times d_a}`$ 를 정답으로 두고, 가벼운 예측 헤드 $`g_\phi`$ 가
$`\mathbf{M}`$ 에서 그것을 복원하게 합니다.

```probe-eq
{"read": "검색된 움직임 토큰에서 가벼운 헤드가 예측한 과거 T_hist 스텝의 행동열이 실제 과거 행동열과 가깝도록, 원소 수로 정규화한 L2 로 맞춘다",
 "tex": "\\mathcal{L}_{\\mathrm{traj}}=\\frac{1}{T_{\\mathrm{hist}}\\,d_a}\\left\\|g_{\\phi}(\\mathbf{M})-\\mathbf{a}_{\\mathrm{hist}}\\right\\|_{2}^{2}",
 "symbols": [
   {"sym": "g_{\\phi}", "name": "보조 예측 헤드", "note": "Stage II 에서는 떼어 냅니다 — 추론 때 없음"},
   {"sym": "\\mathbf{a}_{\\mathrm{hist}}", "name": "이력 구간 행동열", "note": "이력 창과 같은 T_hist 스텝, 로봇이 실제로 낸 행동"},
   {"sym": "\\alpha", "name": "가중치", "note": "값은 논문에 없습니다"}]}
```

정답을 픽셀이나 밀집 궤적이 아니라 **행동열**로 둔 이유를 저자들은 한 줄로 답합니다 — 행동이 로봇의
동역학적 진행과 상태 변화를 더 직접 반영하기 때문입니다. 부록 A.3 은 이 헤드의 역할을 더 좁게 못
박습니다: Stage I 의 정렬 동안 검색된 토큰의 모양을 잡는 데만 쓰이고, Stage II 에서는 떼어 내므로
추론 때 추가 예측 요구가 없습니다.

> [!WARNING] α 의 값이 논문에 없다
> 두 항의 비중 $`\alpha`$ 는 기호로만 등장하고 값이 어디에도 적혀 있지 않습니다. 보조 손실이 절제
> 실험에서 10 포인트를 가르는 항인 만큼(뒤의 Table 5), 재현하려면 이 빈칸을 스스로 채워야 합니다.

```probe-quiz
{"q": "보조 손실의 정답으로 픽셀 대신 과거 행동열을 고른 이유는?",
 "options": ["픽셀 복원은 계산이 너무 비싸서",
             "행동열이 로봇의 동역학적 진행과 상태 변화를 더 직접 반영하기 때문",
             "TraceAnything 이 픽셀 출력을 내지 않기 때문"],
 "answer": 1,
 "why": "§3.3 의 문장 그대로입니다. 계산 비용은 논문이 든 이유가 아니고, TraceAnything 은 밀집 궤적장을 내므로 셋째는 사실과도 어긋납니다."}
```

### 다시 합친다 — Recouple | Token-mixing MLP · Fusion · Shared Self-attention · Two-stage Recipe

마지막 단계는 가볍습니다. 움직임 조건 토큰 $`\mathbf{M}`$ 과 VLA 의 현재 **다시점** 시각 토큰
$`\mathbf{V}^{\mathrm{mv}}\in\mathbb{R}^{B\times S_{\mathrm{mv}}\times E}`$ 를 이어 붙이고
[토큰 혼합 MLP](term:token-mlp) 를 한 번 통과시켜 융합 토큰
$`\mathbf{F}=\mathrm{MLP}([\mathbf{M};\mathbf{V}^{\mathrm{mv}}])\in\mathbb{R}^{B\times(S_{\mathrm{img}}+S_{\mathrm{mv}})\times E}`$
를 만듭니다. $`\mathbf{F}`$ 가 텍스트 토큰·상태 토큰과 함께 백본에 들어가고, 행동 전문가는 공유
자기어텐션을 통해 현재 지각과 검색된 움직임 둘 다를 조건으로 삼습니다.

```probe-term
{"id": "token-mlp", "title": "토큰 혼합 MLP (token-mixing MLP)",
 "body": "이어 붙인 토큰열에 적용하는 얕은 MLP. Table 5 의 'Proj' 열이 이 부품이고, 이것을 빼면 M 이 다시점 토큰과 섞이지 않은 채 들어갑니다. Table 1 의 'w/o Recouple' 행이 정확히 그 변형입니다."}
```

네 부품 중 무엇이 얼려 있고 무엇이 학습되고 무엇이 학습 때만 있는지가 이 설계의 비용 구조를
정합니다.

```probe-parts
{"rows": [
  {"label": "추출 — TraceAnything", "range": "W_t → Z_t", "state": "얼림",
   "body": "과거 창을 궤적장 디코더 토큰으로. 스텝마다 한 번, 가중치는 건드리지 않습니다."},
  {"label": "사영·기억 — W_z, PosEnc", "range": "Z_t → M_kv", "state": "학습",
   "body": "추출기 토큰을 VLA 은닉 차원으로 옮기고 위치를 부여해 키-값 기억으로 만듭니다."},
  {"label": "검색 — 교차어텐션", "range": "V_head, M_kv → M", "state": "학습",
   "body": "현재 헤드캠 토큰이 기억을 질의해 같은 길이의 움직임 조건 토큰을 얻습니다."},
  {"label": "접지 — g_φ 와 L_traj", "range": "M → â_hist", "state": "학습 때만",
   "body": "검색된 토큰에서 과거 행동열을 복원. Stage I 에서만 붙고 Stage II 에서 떼어 냅니다."},
  {"label": "재결합 — 토큰 혼합 MLP", "range": "[M; V_mv] → F", "state": "학습",
   "body": "움직임 토큰과 다시점 토큰을 섞어 백본 프리픽스로 보냅니다."}]}
```

학습은 [일반→특화 두 단계](term:two-stage)입니다. Stage I 은 100 시간짜리 이질 조작 혼합에서
행동 손실과 $`\mathcal{L}_{\mathrm{traj}}`$ 로 움직임 기억 인터페이스를 VLA 임베딩 공간에
**정렬**하고, Stage II 는 보조 헤드를 뗀 채 목표 과제에 적응합니다.

```probe-term
{"id": "two-stage", "title": "generic-to-specific 두 단계 학습",
 "body": "Stage I — 100 시간 이질 조작 혼합에서 행동 손실 + 보조 궤적 손실로 새 인터페이스를 정렬. Stage II — 보조 헤드를 떼고 목표 과제에 적응. 두 단계 모두 학습률 1e-5, 배치 32 이고, Stage I 의 길이가 성능을 좌우한다는 것이 부록 A.1 의 발견입니다."}
```

::: details 구현 설정 (원문 §4.1, 부록 A.1)
| 항목 | 값 |
|---|---|
| 백본 | π0 (PaliGemma-3B + 플로우 매칭 행동 전문가) |
| 헤드캠 스트림 | 10 FPS |
| 이력 창 | $`T_{\mathrm{hist}}=5`$, $`\Delta=2`$ — 원시 프레임 10 장 |
| 이력 부족 시 | 가장 이른 프레임으로 왼쪽 패딩, 학습·추론 동일 |
| 궤적 추출기 | TraceAnything, 얼림, 스텝당 1 회 |
| Stage I | 이질 조작 혼합 100 시간, $`\mathcal{L}_{\mathrm{action}}+\alpha\mathcal{L}_{\mathrm{traj}}`$ |
| Stage II | 목표 과제, 보조 헤드 분리 |
| 학습률 · 배치 | 1e-5 · 32 (두 단계 공통) |
| 실기 데이터 | Agilex Piper, 과제당 100 개 이상 시연 |
| 실기 학습 · 평가 | NVIDIA H20 8 장 학습 · A800 1 장 평가 |
| 명시되지 않은 것 | VLM 백본의 동결 여부, $`\alpha`$, $`S_{\mathrm{4D}}`$, Stage II 스텝 수 |
:::

```probe-quiz
{"q": "추론 시점에 실제로 돌아가는 새 부품은?",
 "options": ["추출기 · 사영 · 교차어텐션 · 토큰 혼합 MLP — 보조 헤드는 없음",
             "추출기 · 사영 · 교차어텐션 · 보조 헤드 · 토큰 혼합 MLP 전부",
             "교차어텐션 하나"],
 "answer": 0,
 "why": "부록 A.3 이 명시합니다 — 보조 궤적 헤드는 Stage II 에서 떼어 내므로 추론 때 추가 예측 요구가 없습니다. 나머지 넷은 매 스텝 돌아야 M_kv 와 F 가 만들어지므로 하나만 남는 것도 아닙니다."}
```

## 3 정말 되는가

### RoboTwin2.0 여섯 과제 — 41 에서 53, Touch 는 19 에서 41 | RoboTwin2.0 · Long/Mid/Short Horizon · Success Rate

시뮬레이션 첫 번째는 [RoboTwin2.0](term:robotwin) 의 여섯 과제입니다. 에피소드 길이로 묶어 장기
(400 스텝 초과) 둘, 중기(300–400) 둘, 단기(300 미만) 둘이고, 장기 과제 하나인 blocks_touching_rgb 는
다단계 시간 추론을 압박하려고 저자들이 **직접 만든** 과제입니다.

```probe-term
{"id": "robotwin", "title": "RoboTwin2.0",
 "body": "강한 도메인 무작위화를 가진 양팔 조작 시뮬레이션 벤치마크이자 데이터 생성기. 이 논문은 표준 과제 다섯에 자체 제작한 blocks_touching_rgb 를 더해 여섯 과제로 평가하고, 부록 B 에 각 과제의 목표와 성공 기준을 적어 둡니다."}
```

```probe-figure
{"id": "A2.F5", "url": "https://arxiv.org/html/2606.08288v1/tasks.png",
 "caption": "여섯 과제의 시연.",
 "source": "Figure 5, 원문 부록 B"}
```

::: details 여섯 과제의 목표와 성공 기준 (원문 부록 B)
| 과제 | 지평 | 목표 | 성공 기준 |
|---|---|---|---|
| blocks_touching_rgb (자체 제작) | 장기 | 집게 끝으로 빨강 → 초록 → 파랑 순으로 블록을 건드림 | 세 블록을 정확히 한 번씩, 순서대로, 반복·누락 없이 접촉 |
| blocks_ranking_rgb | 장기 | 세 블록을 집어 왼쪽부터 빨강 → 초록 → 파랑 한 줄로 배열 | 대략 일직선 + 좌우 색 순서 일치 |
| stack_blocks_two | 중기 | 초록 블록을 빨강 위에 쌓음 | 초록이 빨강 위에 안정적으로 유지 |
| stack_bowls_three | 중기 | 그릇 셋을 한 무더기로 | 셋이 모두 쌓임 |
| place_a2b_right | 단기 | 무작위 물체 A 를 무작위 물체 B 의 오른쪽에 | A 가 B 의 오른쪽에 위치 |
| handover_block | 단기 | 왼팔이 빨간 정육면체를 잡아 오른팔에 건네고, 오른팔이 파란 패드에 놓음 | 정육면체가 패드 위에 안정적으로 유지 |
:::

평균은 π0 의 41% 에서 **53%** 로 12 포인트 오르고, 가장 긴 Touch 과제는 **19% 에서 41%** 로 두 배가
넘습니다. 비교군인 DP·DP3·RDT·OpenVLA-OFT 는 장기 두 과제에서 거의 0 에 가깝습니다.

```probe-scale
{"title": "RoboTwin2.0 여섯 과제 평균 성공률",
 "rows": [
   {"label": "DP", "n": 16, "value": "16%"},
   {"label": "RDT", "n": 19, "value": "19%"},
   {"label": "OpenVLA-OFT", "n": 33, "value": "33%"},
   {"label": "DP3", "n": 34, "value": "34%"},
   {"label": "π0 (베이스라인)", "n": 41, "value": "41%"},
   {"label": "MotionVLA w/o Recouple", "n": 51, "value": "51%"},
   {"label": "MotionVLA", "n": 53, "value": "53%", "us": true}]}
```

::: details Table 1 — RoboTwin2.0 성공률 전문 (%)
| 방법 | Touch (장) | Rank (장) | Stack2 (중) | Stack3 (중) | Place (단) | Hand (단) | 평균 |
|---|---|---|---|---|---|---|---|
| DP (RSS'23) | 0 | 0 | 7 | 63 | 13 | 10 | 16 |
| DP3 (arXiv'24) | 0 | 3 | 24 | 57 | 49 | 70 | 34 |
| RDT (ICLR'25) | 0 | 1 | 25 | 48 | 0 | 42 | 19 |
| OpenVLA-OFT (arXiv'25) | 3 | 45 | 53 | 62 | 23 | 11 | 33 |
| π0 (RSS'25) | 19 | 43 | 43 | 55 | 35 | 48 | 41 |
| Ours (w/o Recouple) | 37 | 53 | 52 | 63 | **45** | 55 | 51 |
| Ours (w/ Recouple) | 41 | 53 | 57 | 67 | 37 | 63 | 53 |
:::

같은 표를 열별로 읽으면 재결합이 **모든 과제에서 이득은 아니라는** 것이 보입니다. 단기 과제 Place
에서는 재결합을 뺀 변형이 45% 로 가장 높고, 재결합을 넣으면 37% 로 8 포인트 내려옵니다 — π0 의 35%
에 거의 붙습니다. 논문은 이 열을 따로 설명하지 않지만, §6 의 "현재 관찰로 충분할 때는 중립이거나
약간 해로울 수 있다"는 문장이 정확히 이 칸을 가리키는 것 같아요. 그리고 DP3 이 Hand 에서 70% 로
모든 VLA 를 크게 앞선다는 것도 표에 그대로 있습니다.

```probe-quiz
{"q": "Table 1 에서 재결합(Recouple)이 오히려 성능을 낮춘 과제는?",
 "options": ["장기 과제 Touch",
             "단기 과제 Place — 45% 에서 37% 로",
             "그런 과제는 없다"],
 "answer": 1,
 "why": "Place 열에서 w/o Recouple 45, w/ Recouple 37 입니다. Touch 는 37 에서 41 로 올랐고, 나머지 네 과제도 같거나 올랐으므로 '없다'는 틀립니다."}
```

### LIBERO 95.4 — 그리고 4D-VLA 보다 경로가 곧다 | LIBERO · Path Efficiency · Motion Consistency

두 번째 시뮬레이션은 [LIBERO](term:libero) 네 스위트입니다. 평균 95.4% 로 표 안에서 최고이고,
장기 스위트 LIBERO-Long 은 **91.2%** 로 π0 보다 6.0 포인트 높습니다. 다만 표를 세로로 읽으면
격차가 균일하지 않습니다 — Spatial 은 96.2 로 π0(96.8)·SwiftVLA(97.2)보다 낮고, Object 는 π0 의
98.8 에 못 미치는 98.0 이며, 평균에서 SwiftVLA(95.1)와의 차이는 0.3 포인트입니다. 이득은 **Long 에
집중**돼 있고, 그것이 이 논문의 주장과 맞는 자리이기도 합니다.

```probe-term
{"id": "libero", "title": "LIBERO",
 "body": "Spatial · Object · Goal · Long 네 스위트로 이뤄진 조작 시뮬레이션 벤치마크. 이 논문은 표준 프로토콜로 스위트별 성공률과 평균을 보고하고, Goal 과 Long 두 스위트에서만 경로 효율을 추가로 잽니다."}
```

::: details Table 2 — LIBERO 성공률 전문 (%)
History 는 추론 때 과거 프레임을 명시적으로 조건화하는지, 3D 는 추론 때 명시적 3D 단서(깊이·점군·3D 좌표 임베딩)를 쓰는지입니다.

| 방법 | History | 3D | Spatial | Object | Goal | Long | 평균 |
|---|---|---|---|---|---|---|---|
| Diffusion Policy | ✗ | ✗ | 78.3 | 92.5 | 68.3 | 50.5 | 72.4 |
| Octo | ✗ | ✗ | 78.9 | 85.7 | 84.6 | 51.1 | 75.1 |
| OpenVLA | ✗ | ✗ | 84.7 | 88.4 | 79.2 | 53.7 | 76.5 |
| CoT-VLA | ✗ | ✗ | 87.5 | 91.6 | 87.6 | 69.0 | 83.9 |
| π0-FAST | ✗ | ✗ | 96.4 | 96.8 | 88.6 | 60.2 | 85.5 |
| PixelVLA | ✗ | ✗ | 88.5 | 90.0 | 85.8 | 82.6 | 86.7 |
| GR00T-N1 | ✗ | ✗ | 94.4 | 97.6 | 93.0 | 90.6 | 93.9 |
| π0 | ✗ | ✗ | 96.8 | 98.8 | 95.8 | 85.2 | 94.2 |
| TraceVLA | ✓ | ✗ | 84.6 | 85.2 | 75.1 | 54.1 | 74.8 |
| SpatialVLA | ✗ | ✓ | 88.2 | 89.9 | 78.6 | 55.5 | 78.1 |
| DepthVLA | ✗ | ✓ | 96.4 | 98.0 | 95.8 | 89.2 | 94.9 |
| 4D-VLA | ✓ | ✓ | 88.9 | 95.2 | 90.9 | 79.1 | 88.6 |
| SwiftVLA | ✓ | ✓ | 97.2 | 96.8 | 97.4 | 89.0 | 95.1 |
| MotionVLA | ✓ | ✓ | 96.2 | 98.0 | 96.2 | **91.2** | **95.4** |

4D-VLA 의 Goal·Long 은 여기서 90.9·79.1 인데 Table 5 에서는 89.8·78.6 으로 다릅니다. 논문은 두
표의 출처 차이를 설명하지 않습니다.
:::

경로 효율은 이 논문이 세운 두 번째 주장 — 성공률만이 아니라 **실행이 더 곧다** — 의 증거입니다.
Goal 과 Long 에서 MotionVLA 는 각각 1.05 와 1.10 으로 전문가 길이에 가장 가깝고, π0 의 1.23·1.19,
4D-VLA 의 1.36·1.32 를 모두 앞섭니다. 추정 깊이를 쓴 4D-VLA* 가 1.88·2.06 으로 가장 나쁜 것을
두고 저자들은 프레임별 기하 노이즈가 움직임 정합성을 높이기는커녕 **우회를 만든다**고 읽습니다.

> [!TIP] 성공률과 경로 길이가 같은 방향으로 움직였다
> Goal 에서 96.2% 성공에 경로 1.05, Long 에서 91.2% 에 1.10. 성공률이 가장 높은 방법이 경로도
> 가장 짧았고, 4D 를 넣고도 성공률이 낮은 4D-VLA 는 경로도 가장 길었습니다. 이력이 도움이 되는지가
> 아니라 **어떤 이력이 도움이 되는지**를 가르는 표입니다.

```probe-figure
{"id": "A3.F7", "url": "https://arxiv.org/html/2606.08288v1/libero6frames.png",
 "caption": "LIBERO 궤적장 시각화. 연속 프레임이 과거 관찰 창에서 추출된 시간 구조를 가진 움직임 단서를 보여 줍니다.",
 "source": "Figure 7, 원문 부록 C"}
```

```probe-quiz
{"q": "LIBERO 결과에서 MotionVLA 의 이득이 집중된 곳과, 그렇지 않은 곳은?",
 "options": ["네 스위트 모두에서 고르게 1 등이다",
             "Long 에서 91.2 로 가장 크게 앞서지만, Spatial 은 π0 와 SwiftVLA 보다 낮다",
             "Spatial 에서만 앞서고 Long 에서는 뒤진다"],
 "answer": 1,
 "why": "Table 2 에서 Spatial 96.2 는 π0 96.8, SwiftVLA 97.2 아래이고, Object 98.0 도 π0 98.8 아래입니다. Long 91.2 와 평균 95.4 만 최고입니다. 이력이 필요한 장기 과제에 이득이 몰려 있다는 것이 논문의 주장과 맞는 그림입니다."}
```

### 과거 프레임을 그대로 넣으면 37 이 13 이 된다 — 부품 증거 | Component Ablation · Stage-I Alignment · Auxiliary Loss · Projector

이 논문의 가장 강한 표는 절제 실험입니다. Touch 와 Stack3 두 과제에서 부품을 하나씩 켜며 재는데,
첫 두 줄이 논문 전체의 논지를 만듭니다. **순진한 시각 이력**을 넣으면 두 과제 평균이 37% 에서
**13%** 로 무너지고, 정렬 없이 4D 토큰을 넣어도 16% 입니다. 이력이 많으면 좋다는 가정이 여기서
깨집니다.

| 시각 이력 | 4D 토큰 | Stage I 정렬 | 보조 손실 | 토큰 혼합 MLP | Touch | Stack3 | 평균 |
|---|---|---|---|---|---|---|---|
| ✗ | ✗ | ✗ | ✗ | ✗ | 19 | 55 | 37 |
| ✓ | ✗ | ✗ | ✗ | ✗ | 3 | 23 | **13** |
| ✓ | ✓ | ✗ | ✗ | ✗ | 12 | 19 | 16 |
| ✓ | ✓ | ✓ | ✗ | ✗ | 34 | 45 | 40 |
| ✓ | ✓ | ✓ | ✓ | ✗ | 37 | 63 | 50 |
| ✓ | ✓ | ✓ | ✓ | ✓ | 41 | 67 | **54** |

거기서 Stage I 정렬이 40% 로 끌어올리고, 보조 궤적 접지가 50%, 토큰 혼합 재결합이 54% 로 마무리합니다.
저자들의 읽기는 "이력이나 토큰이 더 많아서"라는 설명을 이 표가 배제한다는 것입니다 — 움직임 기억은
**정렬되고, 현재 관찰에 질의되고, VLA 토큰 공간에 다시 주입**되어야 쓸모가 생깁니다.

> [!TIP] 부품 하나당 몇 포인트인가
> 순진한 이력 −24, 4D 토큰 +3, Stage I 정렬 +24, 보조 접지 +10, 재결합 +4 (두 과제 평균). 가장 큰
> 두 항이 "무엇을 넣느냐"가 아니라 **정렬**과 **접지**라는 점이, 기억을 제어 인터페이스로 보자는 §1 의
> 주장이 숫자로 돌아온 자리입니다.

Stage I 의 길이도 변수입니다. 부록 A.1 의 스윕에서 정렬은 **60k 스텝 근방에서 정점**을 찍고, 부족하면
궤적장 특징을 덜 쓰고 지나치게 길면 하류 적응이 약간 나빠집니다. 저자들은 인터페이스를 VLA 임베딩
공간에 정렬하되 **이질 사전학습 혼합에 과특화되기 전에** 멈춰야 한다고 요약합니다. 같은 그림의 왼쪽은
stack_blocks_two 에서 MotionVLA 가 학습 초반부터 더 높은 성공률에 도달하는 것을 보여 주는데,
움직임 기억이 시각적으로 닮은 중간 상태들을 행동에 대응시키는 모호함을 줄여 준다는 것이 저자들의
해석입니다.

```probe-figure
{"id": "A1.F3", "url": "https://arxiv.org/html/2606.08288v1/fig3_efficiency.svg",
 "caption": "학습 효율과 Stage I 민감도. (a) stack_blocks_two 에서 MotionVLA 는 베이스라인보다 이른 시점에 더 높은 성공률에 도달합니다. (b) Stage I 정렬은 이 설정에서 60k 스텝 근방이 정점이며, 정렬 부족은 궤적장 특징을 덜 쓰게 하고 지나치게 긴 Stage I 은 하류 적응을 약간 해칩니다.",
 "source": "Figure 3, 원문 부록 A.1"}
```

```probe-figure
{"id": "A1.F4", "url": "https://arxiv.org/html/2606.08288v1/loss.png",
 "caption": "움직임 기억 인터페이스 정렬 중의 학습 곡선. Stage I 에서 쓰이는 행동 손실과 보조 궤적 접지 손실.",
 "source": "Figure 4, 원문 부록 A.1"}
```

```probe-quiz
{"q": "절제 표에서 가장 큰 두 이득을 만든 부품은?",
 "options": ["4D 토큰 추가와 토큰 혼합 MLP",
             "Stage I 정렬(+24)과 보조 궤적 접지(+10)",
             "시각 이력 추가와 4D 토큰 추가"],
 "answer": 1,
 "why": "16 → 40 이 Stage I, 40 → 50 이 보조 손실입니다. 4D 토큰은 13 → 16 으로 3 포인트, 재결합은 50 → 54 로 4 포인트에 그치고, 시각 이력 추가는 37 → 13 으로 오히려 가장 큰 손실입니다."}
```

### 다섯 장 × 보폭 2 — 창을 더 늘리면 오히려 떨어진다 | History Window · Stride · Inference FPS

부록 A.2 는 창의 길이와 보폭을 쓸어 봅니다. 기준은 blocks_touching_rgb 하나이고, 유효 범위는
$`T_{\mathrm{hist}}\Delta`$ 원시 프레임입니다.

| $`T_{\mathrm{hist}}`$ | $`\Delta`$ | 유효 프레임 | FPS | 성공률 (%) |
|---|---|---|---|---|
| 0 | – | – | 12.3 | 19 |
| 3 | 2 | 6 | 8.9 | 33 |
| 5 | 1 | 5 | 7.3 | 35 |
| **5** | **2** | **10** | **7.1** | **41** |
| 10 | 1 | 10 | 3.1 | 40 |
| 10 | 2 | 20 | 3.1 | 23 |
| 20 | 2 | 40 | 0.9 | 26 |

두 가지가 읽힙니다. 첫째, 같은 다섯 장이라도 보폭을 1 에서 2 로 늘리면 35% 가 41% 로 오르고 속도는
거의 그대로입니다 — 촘촘한 다섯 장보다 **성긴 열 프레임**이 낫습니다. 둘째, 유효 범위를 20·40 프레임으로
늘리면 23%·26% 로 **베이스라인 수준까지** 떨어집니다. 저자들의 설명은 오래되거나 과제와 무관한
초반 움직임이 섞여 검색이 현재 제어 상태에 덜 집중하게 된다는 것입니다. 같은 열 프레임을 10 장으로
촘촘히 넣은 행(10, 1)이 40% 로 (5, 2)와 거의 같으면서 FPS 만 7.1 에서 3.1 로 떨어지는 것도 눈에
띕니다.

> [!WARNING] 이력 가지가 12.3 FPS 를 7.1 로 낮춘다
> 이력 없는 π0 는 12.3 FPS, 기본 설정은 7.1 FPS 입니다. 창을 10 장으로 늘리면 3.1, 20 장이면 0.9 —
> 헤드캠이 10 FPS 이므로 기본 설정만이 스트림을 따라잡습니다. 얼린 추출기를 스텝마다 돌리는 비용이
> 백본이 아니라 이력 쪽에서 나온다는 뜻입니다.

```probe-quiz
{"q": "창을 유효 20 프레임 이상으로 늘렸을 때 성능이 떨어지는 이유로 저자들이 든 것은?",
 "options": ["FIFO 버퍼가 넘쳐 프레임이 유실되기 때문",
             "오래되거나 과제와 무관한 초반 움직임이 섞여 검색이 현재 제어 상태에 덜 집중하게 되기 때문",
             "TraceAnything 이 10 프레임 넘는 입력을 받지 못하기 때문"],
 "answer": 1,
 "why": "부록 A.2 의 문장 그대로입니다. 버퍼 유실은 논문에 없고, 추출기는 20·40 프레임 설정에서도 실제로 돌아가 결과를 냈으므로 입력 길이 제한도 아닙니다."}
```

### 실기 Agilex Piper — 28.7 에서 41.9, 그리고 Touching 은 15% | Agilex Piper · Real-world · Completion Steps

실기는 [Agilex Piper](term:piper) 한 대에 시간적으로 까다로운 과제 셋입니다. 평균 성공률은 28.7%
에서 **41.9%** 로, 평균 완료 스텝은 338 에서 **282** 로 줄었습니다(시연은 244). 저자들은 이것을
"인터페이스가 시뮬레이션 밖으로 옮겨 간다는 예비 증거"로만 읽고, 임베디먼트와 환경을 아우르는 배포
연구가 아니라고 미리 선을 긋습니다.

```probe-term
{"id": "piper", "title": "Agilex Piper",
 "body": "실기 검증에 쓰인 팔. 과제당 100 개 이상의 시연을 모아 H20 8 장에서 학습하고 A800 1 장에서 평가했습니다. 완료 스텝은 완료 시간에 10 을 곱해 반올림한 값이라 헤드캠 10 FPS 기준의 프레임 수와 같습니다."}
```

| 방법 | Pick&Place SR | 스텝 | Ranking SR | 스텝 | Touching SR | 스텝 | 평균 SR | 평균 스텝 |
|---|---|---|---|---|---|---|---|---|
| 시연 | – | 119 | – | 351 | – | 261 | – | 244 |
| π0 | 80.0 | 118 | 6.1 | 558 | 0.0 | – | 28.7 | 338 |
| MotionVLA | 92.5 | 119 | 18.2 | 443 | 15.0 | 285 | 41.9 | 282 |

숫자를 그대로 읽으면 두 얼굴이 있습니다. Pick&Place 는 80 에서 92.5 로 오르고 스텝은 시연과 같은
119 입니다. 그런데 시간 구조가 있는 두 과제의 절대치는 낮습니다 — Ranking 18.2%, Touching 15%. π0 가
Touching 에서 0% 라 스텝이 없으니, π0 의 평균 스텝 338 은 두 과제 평균이고 MotionVLA 의 282 는 세
과제 평균이라는 것도 표 주석에 적혀 있습니다. 즉 "338 → 282" 는 같은 과제 집합 위의 비교가 아닙니다.

```probe-figure
{"id": "A3.F8", "url": "https://arxiv.org/html/2606.08288v1/real6frames.png",
 "caption": "실기 궤적장 시각화. 연속된 Agilex Piper 관찰이 움직임 기억 인터페이스가 쓰는 궤적장 단서를 보여 줍니다.",
 "source": "Figure 8, 원문 부록 C"}
```

```probe-figure
{"id": "A3.F9", "url": "https://arxiv.org/html/2606.08288v1/real.png",
 "caption": "실기 롤아웃. 예비 실기 검증 과제들에서의 Agilex Piper 예시 롤아웃.",
 "source": "Figure 9, 원문 부록 C"}
```

```probe-quiz
{"q": "실기 표의 '평균 스텝 338 → 282' 를 그대로 비교하면 안 되는 이유는?",
 "options": ["스텝이 완료 시간에 10 을 곱한 근사치라서",
             "π0 는 Touching 에서 성공이 없어 두 과제 평균이고, MotionVLA 는 세 과제 평균이라 과제 집합이 다르기 때문",
             "시연 스텝이 과제마다 다르기 때문"],
 "answer": 1,
 "why": "Table 3 주석이 '사용할 수 없는 과제별 스텝은 평균에서 제외'라고 적고, π0 의 Touching 스텝은 '–' 입니다. 근사치라는 점과 시연 길이 차이는 두 방법에 똑같이 적용되므로 비교를 무효로 만들지 않습니다."}
```

### 이력 가지만 망가뜨렸을 때 — 0.5, 3.5, 9.0 포인트 | History Corruption · Bounded Robustness · Extractor Failure

얼린 추출기는 가림·빠른 움직임·겉모습 변화·무질감 영역에서 틀릴 수 있습니다. 부록 A.4 는 그 영향을
통제된 형태로 잽니다. 깨끗한 입력으로 학습한 체크포인트를 그대로 두고, 시험 때 **추출기에 들어가는
과거 프레임만** 망가뜨립니다 — 현재 관찰·고유수용·언어는 그대로. 무작위 공간 가림, 프레임별 겉모습
교란, 이력 프레임 무작위 탈락을 섞어 약·중·강 세 수준으로 올립니다.

| 모델 | 손상 | Rank | Hand | 평균 |
|---|---|---|---|---|
| π0 | 없음 | 43 | 48 | 45.5 |
| MotionVLA | 없음 | 53 | 63 | 58.0 |
| MotionVLA | 약 | 53 | 62 | 57.5 |
| MotionVLA | 중 | 50 | 59 | 54.5 |
| MotionVLA | 강 | 45 | 53 | 49.0 |

평균은 0.5, 3.5, 9.0 포인트씩 **점진적으로** 떨어지고, 강한 손상에서도 두 과제 모두 깨끗한 π0 위에
남습니다. 저자들은 이것을 "이 통제된 이력 가지 손상 설정 아래의 유계 강건성"이라고만 부르고, 심한
추출기 실패는 여전히 행동 품질을 깎는다고 함께 적습니다.

```probe-figure
{"id": "A3.F6", "url": "https://arxiv.org/html/2606.08288v1/success.png",
 "caption": "blocks_touching_rgb 의 정성적 예. 성공(위): 집게가 요구된 순서로 블록을 건드립니다. 실패(아래): 집게가 빨간 블록을 놓치고 접촉 중에 초록·파랑 블록을 건드려 흐트러뜨립니다.",
 "source": "Figure 6, 원문 부록 C"}
```

> [!WARNING] 저자가 스스로 적어 둔 조건들
> **(1) 언제 도움이 되는가** — 시간적 모호함이나 장기 상태 혼동이 있을 때이고, 현재 관찰로 충분하면
> 중립이거나 약간 해로울 수 있습니다. **(2) 추출기** — 가림·빠른 움직임·무질감 장면에서 실패할 수
> 있습니다. **(3) 탐색 범위** — 더 넓은 창 크기 탐색과 더 강한 백본 통합은 향후 과제입니다. **(4) 실기**
> — Agilex Piper 소수 과제의 예비 결과로, 인터페이스가 전이된다는 증거이지 배포 연구가 아닙니다.

```probe-quiz
{"q": "부록 A.4 의 손상 실험이 격리하려는 것은?",
 "options": ["현재 관찰 경로의 시각 잡음 민감도",
             "움직임 기억 가지의 민감도 — 추출기 입력만 망가뜨리고 현재 관찰·상태·언어는 그대로 둔다",
             "언어 지시가 틀렸을 때의 복원력"],
 "answer": 1,
 "why": "본문이 '이 개입은 이력 가지의 민감도를 현재 관찰 경로에서 격리한다'고 적습니다. 현재 관찰과 언어는 손대지 않는 조건이므로 나머지 둘은 이 실험이 재지 않는 것입니다."}
```

## 4 연구축에 무엇이 걸리나

### 동역학 인식 인코더가 현재 관찰이 아니라 이력 앞에 붙었다 | D9NB · D2DV · D8EJ · D3WV

여기부터는 논문이 아니라 **이 결정들에 미치는 영향**입니다.

P2 의 인코더 결정은 동역학 인식 스템(DynaFLIP 계열)이나 기하 증류 스템(eVGGT)을 일반 SigLIP 스템보다
선호한다고 적어 두었습니다(D9NB). 그 문장이 그리는 그림은 **현재 관찰이 지나가는 인코더** 를 바꾸는
것입니다. MotionVLA 는 현재 관찰의 인코더는 π0 것을 그대로 두고, 동역학 인식 인코더를 **이력 창
앞에** 따로 세웁니다 — 얼린 궤적장 추출기가 그것입니다. 같은 성질을 다른 자리에 둔 것이고, 그 자리
차이가 비용 구조를 가릅니다: 현재 경로는 손대지 않으니 사전학습 자산이 보존되고, 대신 스텝마다
추출기 한 번이 붙어 FPS 가 12.3 에서 7.1 로 내려옵니다.

| Decision | 이 기본값 | 이 논문 | 판정 |
|---|---|---|---|
| **D9NB** 동작/동역학 인식 시각 인코더 | 현재 관찰의 스템을 동역학 인식 것으로 교체 | 현재 스템 무변경, 얼린 궤적장 추출기를 이력 창에만 | 방향 지지, 자리 다름 |
| **D2DV** concat 을 넘는 이질 모달리티 융합 | 교차어텐션 / 비대칭 융합, flat concat 반대 | 이력 프레임 concat 은 37 → 13, 교차어텐션 검색 + 토큰 혼합은 54 | 강한 지지 |
| **D8EJ** 다중 카메라 공간-기하 접지 | 다시점을 3D 일관 임베딩으로 등록 | 헤드캠 하나만 궤적장으로, 다시점 토큰은 종전대로 이어 붙임 | 미측정 |
| **D3WV** 단계적 사전학습 + 적응 레시피 | Stage 1 혼합 사전학습 → Stage 3 보수적 적응 | 100 h 혼합 정렬 → 목표 과제 적응, 정렬 60k 초과 시 하류 손실 | 지지, 과특화 수치 하나 추가 |
| **D3RP** 백본 적응 범위 | (a) VLM 전면 동결 + 액션 전문가만 | VLM 동결 여부 미명시 | 미측정 |
| **D9KS** 예측 공간 | 잠재 / 3D flow 예측 | 회고적 3D 궤적장, 예측 없음, 보조 목표는 행동열 | 하한선 후보 |

> [!CAUTION] D2DV — 이 코퍼스에서 가장 선명한 "concat 이 해롭다"의 숫자
> D2DV 는 flat concat 을 안티토픽으로 두고 교차어텐션을 처방으로 적었습니다. 이 논문의 절제 표는 그
> 처방의 두 절반을 같은 두 과제에서 한 번에 잽니다 — 이력 프레임을 이어 붙이면 37 이 13 으로, 교차어텐션
> 검색으로 바꾸면 54 로. TraceVLA 의 −6% 보다 폭이 크고, 이번엔 대안이 렌더링이 아니라 D2DV 가
> 이름 붙인 바로 그 기제입니다(D2DV).

> [!CAUTION] D9NB — "동역학 인식"이 현재 경로의 성질인지 이력 경로의 성질인지 갈라야 한다
> TraceVLA 가 "인코더 가중치 vs 입력 픽셀"이라는 갈래를 열었다면, 이 논문은 셋째 갈래를 엽니다 —
> 현재 관찰 인코더는 그대로 두고 **이력에만** 동역학 인코더를 붙이는 것. 이 시각 인코더 결정은
> 동역학 인식이 어느 경로에 붙는지를 적지 않고 있어서(D9NB), 셋 중 무엇을 고르는지에 따라 사전학습
> 자산과 추론 비용이 전혀 다르게 움직인다는 점이 문장에 없습니다.

> [!CAUTION] D3WV — Stage I 이 길수록 좋은 것이 아니라는 첫 수치
> 이 단계적 레시피는 Stage 1 혼합 사전학습 뒤 Stage 3 보수적 적응을 적어 두었습니다(D3WV). 이 논문의
> Stage I 스윕은 정렬이 60k 근방에서 정점을 찍고 더 길면 하류가 나빠진다는 것을 보입니다 — 새
> 인터페이스를 혼합에 **과특화**시키는 비용입니다. 이 레시피가 "얼마나 오래"를 변수로 갖고 있지
> 않다는 것을 이 곡선 하나가 드러냅니다.

> [!IMPORTANT] 읽은 것들 사이에서 이 논문의 자리
> 이 코퍼스의 이력 논문들은 서로 다른 자리에 섰습니다.
> TraceVLA([arXiv:2412.10345](https://arxiv.org/abs/2412.10345))는 2D 궤적을 **픽셀에 그려** 넣었고,
> 고유수용 상태 연구([arXiv:2608.03052](https://arxiv.org/abs/2608.03052))는 상태 이력이 **짧을 때만
> 약이고 길면 독**이라는 곡선을 냈으며, RoboTTT([arXiv:2607.15275](https://arxiv.org/abs/2607.15275))는
> 8K 스텝 이력을 **가중치에** 눌러 담았습니다. MotionVLA 는 그 사이 — 1 초짜리 3D 움직임을 키-값
> 기억으로 두고 현재가 묻게 하는 자리 — 에 서고, 창을 20 프레임 넘게 늘리면 떨어진다는 표로 짧은-이력
> 곡선을 한 번 더 확인합니다.

```probe-quiz
{"q": "이 논문이 D9NB 에 새로 여는 갈래는?",
 "options": ["동역학 인식 인코더가 필요 없다는 것",
             "현재 관찰 인코더는 그대로 두고 이력 경로에만 동역학 인코더를 붙이는 셋째 자리가 있다는 것",
             "SigLIP 스템을 TraceAnything 으로 바꿔야 한다는 것"],
 "answer": 1,
 "why": "절제 표가 동역학 정보의 쓸모를 강하게 지지하므로 첫 항목은 반대입니다. 셋째는 이 논문이 하지 않은 일입니다 — 현재 관찰의 스템은 π0 그대로입니다. 새로 생기는 것은 같은 인코더를 어느 경로에 붙이느냐는 자리 선택입니다."}
```

### 손 안에서 궤적장은 무엇을 추적하는가 | Verification Plan · Occlusion · Head-camera Only · Latency Budget

이 방법이 이 스택으로 곧장 넘어오지 않는 지점도 분명합니다. MotionVLA 의 움직임은 전부 **헤드
카메라에 보이는 팔과 물체의 거시 운동**입니다. 이 손(22-DOF, 손목 DOF 없음, 지문 촉각)의 1 단계
데모인 손바닥 안 회전에서는 팔이 거의 서 있고, 움직이는 것은 손가락과 물체 표면이며, 그 표면은
손가락에 가려집니다. 저자들이 적은 추출기의 실패 조건 첫 번째가 바로 **가림**입니다.

> [!CAUTION] D9KS — 회고적 3D 궤적장이 예측적 3D flow 의 하한선이 된다
> P3 의 예측 공간 결정은 잠재 / 3D flow 를 골랐습니다(D9KS). TraceVLA 가 회고적 2D flow 로 세운
> 하한선을 이 논문은 **3D** 로 올려놓습니다 — 미래를 예측하지 않고 지난 1 초의 3D 움직임을 읽기만
> 해도 LIBERO-Long 이 6 포인트, RoboTwin 이 12 포인트 오릅니다. 세계 모델의 이득을 잴 때 이 회고
> 3D 기준선을 빼지 않으면, 예측 능력의 몫과 3D 움직임 정보 자체의 몫이 섞입니다.

> [!CAUTION] D3RP — 동결 조건이 적혀 있지 않은 이득
> 이 적응 범위는 (a) VLM 전면 동결 + 액션 전문가만 학습입니다(D3RP). 이 논문은 얼린 것이 추출기뿐이라고
> 적고 VLM 백본의 동결 여부는 말하지 않습니다. 새 부품이 전부 프리픽스 앞단에 붙고 백본이 그 토큰
> 분포를 배워야 하는 구조라, 동결 조건에서 같은 이득이 서는지는 이 논문으로는 알 수 없습니다.

#### 먼저 확인할 것

- **가장 싼 점검 — 이 데이터에서 궤적장이 무엇을 잡는가.** 손바닥 안 회전 시연의 헤드캠 1 초 창에
  TraceAnything 을 돌려 Figure 7·8 같은 시각화를 뽑아 보는 것만으로 답이 나옵니다. 손가락 궤적만
  잡히고 물체 표면이 비면, 이 기억은 "손이 어떻게 움직였나"는 담아도 "물체가 어떻게 돌았나"는 담지
  못합니다. 코드도 데이터도 있는 점검입니다.
- **순진한 이력 대조군을 우리 π0 위에서 먼저 재현할 것.** 37 → 13 은 이 논문의 과제·백본에서 나온
  숫자입니다. 이 스택에 어떤 이력이든 넣기 전에, 프레임 concat 이 여기서도 깎는지를 한 번 재면 D2DV
  의 처방이 이 데이터에서 성립하는지가 먼저 확인됩니다(D2DV).
- **동결 조건에서 다시 잴 것.** VLM 을 동결하고(D3RP) 사영·교차어텐션·MLP 만 학습하는 (a) 설정에서
  이득이 남는지. 사라진다면 이 방법은 그 동결을 흔드는 비용을 요구하는 것이고, 그때는 이력 경로
  인코더와 현재 경로 인코더 교체(D9NB)를 정면으로 비교해야 합니다.
- **보조 목표의 정답을 무엇으로 둘지.** 이 논문의 $`\mathcal{L}_{\mathrm{traj}}`$ 는 과거 행동열을
  $`T_{\mathrm{hist}} d_a`$ 로 나눠 평균합니다. 팔 6 + 손 22 를 한 벡터에 넣으면 손가락 항이 수로
  지배하는데, 이것이 접지에 유리한지 불리한지는 이 논문이 답할 수 없는 질문입니다 — 이 Body/Hand
  분리(D3GS) 위에서는 정답열도 둘로 나뉘는 것이 자연스러운 것 같아요.
- **지연 예산에 이력 가지를 어디에 둘지.** 기본 설정도 12.3 → 7.1 FPS 이고, 추출기는 스텝마다 돕니다.
  이 스택은 25 Hz 접촉 반응을 정책 루프 안에서 보려 하므로(D5DQ), 1 초 창의 궤적장은 행동 루프와 같은
  주기로 돌 이유가 없습니다 — 비동기로 갱신하는 설계가 성립하는지가 먼저입니다.

**아직 아무도 답하지 않은 질문** — 저자들 스스로 "현재 관찰로 충분하면 중립이거나 약간 해롭다"고
적었고, Place 열의 45 → 37 이 그 예입니다. 접촉이 많은 손 안 조작에서 모호함의 정체는 **시간**
(어디까지 했는가)인가, **모달리티**(지금 무엇이 눌리고 있는가)인가. 두 종류의 모호함을 같은 과제
위에서 갈라 잰 논문은 이 코퍼스에 없습니다. 이력이 답이 되는 것은 첫째일 때뿐이고, 둘째라면 답은
D3AG 의 촉각 토큰 쪽에 있습니다. 22-DOF 손으로 넘어가면 정면으로 부딪힐 질문입니다.

```probe-quiz
{"q": "이 방법을 손바닥 안 회전 과제로 옮길 때 가장 먼저 깨지는 전제는?",
 "options": ["π0 가 22-DOF 행동 공간을 다루지 못한다",
             "헤드캠에 보이는 거시 운동에서 궤적장이 잡힌다는 전제 — 손 안 회전에서는 물체 표면이 손가락에 가려진다",
             "TraceAnything 이 실기 영상에서 돌지 않는다"],
 "answer": 1,
 "why": "저자들이 첫 번째로 든 추출기 실패 조건이 가림이고, 손 안 조작은 그 조건이 상시 성립하는 과제입니다. 행동 공간 크기는 백본 쪽 변수이며, 실기 영상에서 추출기가 도는 것은 Figure 8 이 이미 보여 줍니다."}
```

::: glance

```probe-hub
{"thesis": "과거를 프레임으로 기억하면 37 이 13 으로 무너지고, 프레임 사이의 움직임으로 기억하면 54 가 된다",
 "line": "직전 1 초의 헤드캠 프레임을 얼린 궤적장 추출기로 토큰화해 키-값 기억으로 두고, 현재 프레임이 교차어텐션으로 묻게 한 뒤 과거 행동을 복원시켜 접지합니다.",
 "figure": "S2.F2",
 "caption": "과거 전용 창 → 궤적장 토큰 → 현재 프레임의 교차어텐션 검색 → 다시점 토큰과 재결합, 그리고 학습 때만 붙는 과거 행동 복원 헤드.",
 "facts": [{"k": "RoboTwin2.0 평균", "v": "41% → 53%"},
           {"k": "LIBERO-Long", "v": "85.2 → 91.2"},
           {"k": "경로 효율 (Goal)", "v": "1.23 → 1.05"},
           {"k": "이력 프레임 concat", "v": "37% → 13%"}]}
```

긴 조작 과제에서 로봇이 헷갈리는 순간은 대개 화면이 똑같이 보일 때예요. 집게가 블록 위에 떠 있는 장면은 "이제 닿을 차례"일 수도, "이미 닿았으니 다음으로"일 수도 있는데, 현재 한 장만 보는 정책은 그 둘을 가를 방법이 없습니다.

그러니 과거를 보여 주자는 건 자연스러운 처방이죠. 그런데 저자들이 과거 프레임을 그대로 이어 붙여 봤더니 두 과제 평균이 37% 에서 13% 로 떨어졌습니다. 프레임마다 따로 3D 로 띄운 토큰을 넣는 최근 방식도 이들의 측정에서는 과제를 끝내고도 전문가보다 1.36 배 긴 길을 돌아갔고요.

그래서 이 논문의 질문은 "이력을 줄 것인가"가 아니라 "이력을 어떤 형태로 줄 것인가"가 됩니다. 프레임들을 기억할 게 아니라, 프레임들을 잇는 움직임을 기억하자는 거예요.

만드는 법은 이렇습니다. 헤드 카메라의 직전 1 초, 열 프레임에서 한 장 걸러 다섯 장을 고르고(현재 프레임은 일부러 뺍니다), 영상을 시간 연속 3D 궤적장으로 표현하는 얼린 모델에 통째로 넣습니다. 픽셀마다 나오는 밀집 궤적은 버리고 디코더 마지막 층의 토큰만 남겨 정책의 차원에 맞춥니다.

그 토큰들은 프리픽스에 붙지 않아요. 키와 값으로만 놓아두고, 현재 프레임의 시각 토큰이 교차어텐션으로 그 기억을 읽습니다. 그래서 출력 길이는 현재 프레임 토큰 수와 똑같고, 이력이 길어져도 문맥은 늘어나지 않습니다.

한 가지 보험이 더 붙습니다. 그렇게 읽어 온 토큰이 "프레임 사이의 정적 차이"로 주저앉지 않도록, 학습 첫 단계에서만 가벼운 헤드를 달아 같은 1 초 동안 로봇이 실제로 냈던 행동열을 복원하게 합니다. 두 번째 단계에서는 그 헤드를 떼니 추론 때 비용은 없습니다.

결과는 RoboTwin2.0 여섯 과제 평균 41% 에서 53%, 가장 긴 과제는 19% 에서 41% 입니다. LIBERO 에서는 장기 스위트가 85.2 에서 91.2 로 오르고, 말단이 움직인 길이는 전문가의 1.05 배까지 내려와 4D 를 넣은 다른 방법보다 훨씬 곧게 갑니다. (짧은 스위트에서는 π0 보다 살짝 낮은 칸도 있어요.)

절제 표가 가장 재밌는데, 정렬 없이 4D 토큰만 넣으면 16% 이고, 혼합 데이터로 인터페이스를 먼저 정렬하면 40%, 행동 복원으로 접지하면 50%, 다시점 토큰과 섞어 넣으면 54% 입니다. 이력의 양이 아니라 정렬과 접지가 점수를 만든 셈이죠.

값도 있습니다. 이력 없는 π0 가 12.3 FPS 일 때 기본 설정은 7.1 FPS 이고, 창을 20 프레임 넘게 늘리면 성능이 다시 베이스라인 근처로 떨어집니다. 실기 Agilex Piper 세 과제는 평균 28.7% 에서 41.9% 로 올랐지만 시간 구조가 있는 두 과제의 절대치는 18.2% 와 15% 에 머뭅니다.

저자들이 남긴 단서도 그대로 옮기면, 현재 관찰로 충분한 과제에서는 중립이거나 약간 해로울 수 있고, 얼린 추출기는 가림과 빠른 움직임과 무질감 장면에서 틀릴 수 있으며, 실기 결과는 배포 연구가 아니라 예비 증거라는 것입니다.

```probe-rail
{"items": [
  {"k": "읽은 판본", "v": "arXiv 2606.08288v1", "note": "2026-09-09 열람 · 게재 2026-06-06"},
  {"k": "무엇에 올렸나", "v": "π0 (PaliGemma-3B + 플로우 매칭 행동 전문가)", "note": "추출기는 TraceAnything, 얼린 채 스텝당 1 회"},
  {"k": "이력 창", "v": "헤드캠 10 FPS · 5 장 × 보폭 2 = 원시 10 프레임", "note": "현재 프레임 제외, 학습·추론 동일 FIFO 정의"},
  {"k": "얼마나", "v": "RoboTwin2.0 41% → 53% · LIBERO 평균 94.2 → 95.4 · Long 85.2 → 91.2", "note": "경로 효율 Goal/Long 1.05/1.10 (π0 1.23/1.19)"},
  {"k": "어떤 조건에서", "v": "Stage I 혼합 100 h → Stage II 목표 과제, lr 1e-5 · 배치 32", "note": "실기 Agilex Piper 3 과제, 과제당 100+ 시연, H20 8 장 학습 · A800 1 장 평가"},
  {"k": "무엇과 비교했나", "v": "π0 · 4D-VLA · SwiftVLA · DepthVLA · GR00T-N1 외", "note": "여기에 이력 concat · 미정렬 4D 토큰 · Stage I 유무 · 보조 손실 유무 · 재결합 유무 절제"},
  {"k": "저자가 못 한다고 한 것", "v": "현재 관찰로 충분하면 중립~약간 해로움 · 가림·빠른 움직임에서 추출기 실패 · 실기는 예비", "note": "창 크기 탐색과 더 강한 백본 통합은 향후 과제"}]}
```

```probe-act
{"n": 1, "title": "4D 를 넣은 정책이 성공하고도 1.36 배를 돌아간다",
 "claim": "프레임마다 따로 3D 로 띄운 4D 토큰은 과제를 끝내면서도 말단 경로가 전문가보다 훨씬 깁니다. 추정 깊이를 쓰면 1.88 배까지 벌어지고, π0 자체도 1.23 배입니다. 이력의 양이 아니라 정합성이 문제라는 것이 출발점입니다.",
 "scale": {"rows": [
   {"label": "MotionVLA", "n": 1.05, "value": "1.05"},
   {"label": "π0", "n": 1.23, "value": "1.23"},
   {"label": "4D-VLA", "n": 1.36, "value": "1.36"},
   {"label": "4D-VLA* (추정 깊이)", "n": 1.88, "value": "1.88"}]},
 "source": "Table 5, 원문 §4.3 — LIBERO-Goal 경로 효율, 성공 에피소드 평균"}
```

```probe-act
{"n": 2, "title": "현재 프레임이 직전 1 초의 움직임 기억을 묻는다",
 "claim": "과거 전용 열 프레임을 얼린 궤적장 추출기의 디코더 토큰으로 접어 키-값 기억으로 두고, 현재 헤드캠 토큰이 교차어텐션으로 읽습니다. 출력 길이는 현재 토큰 수와 같아 문맥이 늘지 않고, 학습 첫 단계에서만 과거 행동열 복원으로 그 토큰을 접지합니다.",
 "figure": "S2.F2",
 "caption": "과거 전용 창 → 궤적장 토큰 → 교차어텐션 검색 → 다시점 토큰과 재결합, 오른쪽 위가 학습 때만 붙는 복원 헤드.",
 "source": "원문 §3.2–3.3 · Figure 2 · 부록 A.3 (보조 헤드는 Stage II 에서 분리)"}
```

```probe-act
{"n": 3, "title": "프레임 그대로는 13, 정렬·접지·재결합을 거치면 54",
 "claim": "Touch·Stack3 두 과제 평균에서 순진한 시각 이력은 37 을 13 으로 깎고, 미정렬 4D 토큰은 16 입니다. 혼합 데이터 정렬이 40, 과거 행동 복원 접지가 50, 토큰 혼합 재결합이 54 를 만듭니다.",
 "scale": {"rows": [
   {"label": "π0 (이력 없음)", "n": 37, "value": "37%"},
   {"label": "+ 시각 이력 concat", "n": 13, "value": "13%"},
   {"label": "+ 미정렬 4D 토큰", "n": 16, "value": "16%"},
   {"label": "+ Stage I 정렬", "n": 40, "value": "40%"},
   {"label": "+ 보조 궤적 접지", "n": 50, "value": "50%"},
   {"label": "+ 토큰 혼합 재결합", "n": 54, "value": "54%"}]},
 "source": "Table 5, 원문 §4.3 — 부품 절제, Touch·Stack3 평균"}
```

```probe-act
{"n": 4, "title": "창은 10 프레임이 한계이고, 이력 가지를 망가뜨려도 9 포인트만 잃는다",
 "claim": "유효 20·40 프레임으로 늘리면 23%·26% 로 베이스라인 근처까지 떨어지고, 기본 설정은 12.3 FPS 를 7.1 로 낮춥니다. 시험 때 추출기 입력만 약·중·강으로 손상시키면 평균이 0.5·3.5·9.0 포인트씩 점진적으로 내려오고, 강한 손상에서도 깨끗한 π0 위에 남습니다.",
 "scale": {"rows": [
   {"label": "π0 (손상 없음)", "n": 45.5, "value": "45.5%"},
   {"label": "MotionVLA 강한 손상", "n": 49.0, "value": "49.0%"},
   {"label": "MotionVLA 중간 손상", "n": 54.5, "value": "54.5%"},
   {"label": "MotionVLA 약한 손상", "n": 57.5, "value": "57.5%"},
   {"label": "MotionVLA 손상 없음", "n": 58.0, "value": "58.0%"}]},
 "source": "Table 6·7, 원문 부록 A.2·A.4 — blocks_touching_rgb 창 스윕, Rank·Hand 손상 실험"}
```

:::
