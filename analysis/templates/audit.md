# Paper↔Code Audit — <원문 제목 verbatim>

> PROBE paper↔code audit 모드 산출물 (`/audit-paper`). 한글 단일 문서이며,
> 논문이 주장한 method / defaults / metric 이 논문 자체의 공식 repo 와
> 일치하는지 검사한 재현 게이트 보고서입니다. `/reproduce-paper` 투자 전
> 게이트 — `/validate-impl` 의 Design↔foundry 검증과 다른 쌍을 검사합니다.
> 형식·이모지·용어 규칙은 `docs/style.md` §6-6 / §4 를 정확히 따릅니다.
> 재실행 시 이 파일을 덮어씁니다.

---

## 📄 메타

| 항목 | 내용 |
|------|------|
| arXiv id | XXXX.XXXXX |
| 원문 제목 | <verbatim> |
| Claim source | `analysis/<id>/analysis.md` (사전 deep-dive 존재) / 본문 직접 추출 (deep-dive 부재) |
| 공식 레포 | <URL> (<브랜치/태그>) |
| Fetched commit | `<full 40-char SHA>` (해석 불가 시 `unknown (could not resolve)`) |
| Audit 생성일 | YYYY-MM-DD (`TZ=Asia/Seoul`) |
| 검사 항목 | 검사 N · 일치 X · 불일치 Y · 누락 Z |

---

## 🔗 공식 코드

<!-- 무엇을 어떻게 가져왔는지: 트리 확인 방법 (GitHub API / shallow clone),
     실제로 읽은 파일 목록과 각 파일의 역할 한 줄. 읽지 않은 파일은
     아래 어떤 절에서도 인용 금지 (unread = unstated). -->

- `<path/in/repo>` — <역할 한 줄>
- …

---

## ✅ 일치

<!-- 코드가 뒷받침하는 주장. 반드시 `file:line` 또는 config key 를 인용. -->

| # | 논문 주장 | 코드 근거 |
|---|----------|----------|
| 1 | <주장> | `<file>:<line>` / `<config key>: <value>` |
| … | | |

---

## ⚠️ 불일치

<!-- 코드가 논문과 다르게 하는 주장. 심각도별로 plain `###` 헤더
     (`### MAJOR`, `### MINOR`, `### FATAL`) 아래 그룹화 — `###` 에는
     이모지를 쓰지 않습니다 (STYLE §2). 각 항목은 논문 주장 vs 코드 실측을
     나란히 제시. 해당 심각도 항목이 없으면 그 `###` 블록 자체를 생략. -->

### FATAL

**<불일치 한 줄 제목>**

- 논문: <주장 + 출처 (§/Table)>
- 코드: <실측 + `file:line`>
- <재현 영향 한 줄>

### MAJOR

**<불일치 한 줄 제목>**

- 논문: …
- 코드: …
- <재현 영향 한 줄>

### MINOR

**<불일치 한 줄 제목>**

- 논문: …
- 코드: …
- <영향 없음/미미 사유 한 줄>

---

## 🕳️ 누락

<!-- 대응 코드가 아예 없는 주장 (미공개 컴포넌트/브랜치/데이터).
     불일치 (있는데 다름) 와 구분해 유지합니다. -->

| # | 누락 항목 | 상세 |
|---|----------|------|
| 1 | <컴포넌트> | <근거 — README 문구 / 트리 부재 확인 방법> |
| … | | |

---

## ♻️ 재현 리스크

<!-- 주장이 일치해도 재현을 막는 run-blocker: seed 부재, 의존성 미고정,
     하드코딩 절대 경로, 환경/하드웨어 스펙 부재, 비결정 연산. -->

| 항목 | 심각도 | 내용 |
|------|--------|------|
| <리스크> | 높 / 중 / 낮 | <상세 + 근거 `file:line`> |
| … | | |

---

## 🚦 재현 게이트

**<✅ 재현 추천 / ⚠️ 주의 후 재현 / 🛑 재현 비추천 / 🚫 audit 불가>**

<한 줄 rationale.>

<!-- ⚠️ 주의 후 재현일 때는 재현 착수 전 반드시 지켜야 할 주의사항을
     번호 목록으로. 🚫 audit 불가는 사유 (repo 부재/404/private) 한 줄. -->

1. <주의사항>
2. …
