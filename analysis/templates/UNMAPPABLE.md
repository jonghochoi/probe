# UNMAPPABLE — <arXiv id> ↛ `<foundry>`

> `/implement-design` 산출물 (`analysis/<id>/impl/<foundry>/UNMAPPABLE.md`).
> Design 이 이 foundry 의 좌표계로 매핑되지 않아 `impl.md`/`impl.patch` 를
> 생성하지 않은 정직한 종료 기록입니다. 두 경로 중 하나로만 생성됩니다 —
> ① Design 📄 메타의 `Design 적용 | 🚫 비대상` short-circuit,
> ② §A 매핑 실패 (foundry 가 만족시킬 수 없는 구체적 계약/인터페이스).
> 규칙: `docs/style.md` §6-4. 재실행 시 덮어씁니다.

## 🚫 매핑 불가

<!-- 정확히 한 문단. 반드시 포함:
     - 경로 ①이면: Design 의 🚫 비대상 verdict 와 사유 인용.
     - 경로 ②면: foundry 가 만족시킬 수 없는 구체적 계약/인터페이스
       (Design 의 어느 섹션·행이 어느 지점에서 좌초했는지).
     - 경로 ②일 때만, analysis.md 마지막 줄에 아래 blockquote 를 append
       했다는 사실 (경로 ①은 analysis.md 를 건드리지 않음):
       > 🚧 매핑 불가 (`<foundry>`) — Design 의 일부가 이 foundry 의 좌표계로 매핑되지 않습니다.
     추측·완곡어로 채우지 않습니다 — clean UNMAPPABLE 이 fabricated patch
     보다 낫습니다. -->

<한 문단.>
