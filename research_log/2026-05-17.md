# Probe Daily Scouting Report — 2026-05-17

**Run date:** 2026-05-17
**Agent version:** v0.1
**Papers scanned:** ~40 unique papers via `mcp__Hugging_Face__paper_search` (4 keyword queries) + 0 Author Watch (tool unavailable) + 0 Citation-Graph Expansion (skipped, daily cadence) + 0 Competitor Monitoring (skipped, daily cadence)
**Papers surfaced (every dimension ≥ 2):** 2

> Two output files produced: `2026-05-17.md` (English, primary) and `2026-05-17-KO.md` (Korean translation).

---

## 📋 Scout Methodology

- **Author Watch** — TOOL FAILURE: `semantic-scholar.get_author_papers` is not registered in this environment and could not be called (tool schema absent from available tool list). Fallback: WebSearch queries for each tracked researcher group (Physical Intelligence team: Kevin Black, Danny Driess, Karl Pertsch, Lucy Xiaoyang Shi, Allen Z. Ren; Cewu Lu / Lixin Yang; Zhao-Heng Yin; Taku Komura; Toru Lin; Haozhi Qi). No new paper from any tracked researcher was found for May 15–17, 2026. Most recent confirmed PI-team paper: π0.7 ([arXiv:2604.15483](https://arxiv.org/abs/2604.15483), April 16, 2026) — outside the 2-day window.
- **Keyword Sweep** — TOOL FAILURE: `arxiv.search_papers` is not registered in this environment and could not be called. Fallback: `mcp__Hugging_Face__paper_search` executed for 4 queries (dexterous arm-hand heterogeneous action expert; flow-matching VLA action expert; tactile FiLM fusion contact-rich manipulation; body-hand coordinated diffusion gradient conflict decoder). Approximately 40 unique papers surfaced.
- **Date-filter failure**: `arxiv.org/list/cs.RO/pastweek` returned HTTP 403 Forbidden; `huggingface.co/papers?date=2026-05-17` returned HTTP 403 Forbidden; `mcp__Hugging_Face__hf_hub_query` for daily papers returned empty result `{"result":[]}`. Consequence: the strict 2-day date filter (May 15–17, 2026) could NOT be enforced. Actual submission dates are reported per paper; both surfaced papers predate the strict window.
- **Citation-Graph Expansion** — skipped (daily cadence).
- **Competitor Monitoring** — skipped (daily cadence). No new releases from §8 watch list (DexReMoE, LaMP, HEX) were found via WebSearch.
- **P1 Anti-topic filter** applied to all surfaced papers: 5 papers dropped (see 🚫 section). 1 paper (SnapFlow) dropped for Sim2Real = 1 (simulation-only).
- **De-duplication** — no prior reports in `research_log/` (only `_TEMPLATE.md` present). No papers excluded on this basis.

---

## 🥇 Paper 1 — PRIORITY ★★★

**MolmoAct2: Action Reasoning Models for Real-world Deployment**
[arXiv:2605.02881](https://arxiv.org/abs/2605.02881) · Haoquan Fang, Jiafei Duan, Donovan Clay, Sam Wang, Shuo Liu, Weikai Huang, Xiang Fan, Wei-Chuan Tsai, et al. (AllenAI / University of Washington) · submitted: May 4, 2026 · source: keyword-sweep

### 🎯 (a) P# / D# touched

P1 — D7 (flow matching coordination), D8 (sharing module mechanism), D5 (backbone freeze strategy).

D8 is the primary target. MolmoAct2's per-layer KV-cache conditioning of the flow-matching action expert on the VLM backbone is exactly D8's deferred option (C) cross-attention at (β) multi-layer depth. D8.v1 is single-point FiLM with a_b → (γ, β) at (α) single point; MolmoAct2 routes every VLM layer's key-value pairs into the action expert's corresponding layers, demonstrating consistent improvement across 7 simulation and real-world manipulation benchmarks versus simpler conditioning baselines, including π0.5.

D7: MolmoAct2 preserves an independent flow-matching action expert (not coupled denoising), maintaining architectural compatibility with D7.v1 (hierarchical flow: body completes K-step denoising → a_b → hand denoises conditioned on a_b). The per-layer KV-cache conditioning is a purely information-sharing modification, not a denoising schedule change.

D5: The MolmoER backbone is fine-tuned for spatial and embodied reasoning, then frozen during action-expert training — partially supporting D5.v1 (full freeze) but introducing a pre-freeze specialization phase absent from the standard π0/π0.5 protocol.

### ✨ (b) What is genuinely new

Per-layer KV-cache conditioning of a flow-matching continuous-action expert on a 2.3B discrete-token VLM — unlike π0/π0.5 where the action expert attends to a flat pool of VLM tokens at the output layer, MolmoAct2 routes each VLM layer's KV caches to the corresponding action-expert layer, is validated at scale (5 embodiments, 3 open datasets totaling 720+ hours of bimanual data), and is fully open (weights, training code, all data), while outperforming π0.5 on manipulation tasks and GPT-5 on 13 embodied-reasoning benchmarks.

### ⚙️ (c) Decision implication

If per-layer KV-cache conditioning (D8 option (C)+(β)) generalizes from VLM→action-expert to body-expert→hand-expert conditioning: in `hand_head_cfg.py`, replace `sharing_module = "film_single"` with `sharing_module = "cross_attn_per_layer"`. This requires redesigning the hand head from MLP to a small Transformer — the exact architectural trigger listed in D8's deferred (C) candidate. Concrete action item: lock the MLP vs. Transformer hand-head decision before CP1 code entry, because `cross_attn_per_layer` is incompatible with an MLP-style hand head and cannot be retrofitted without re-implementation.

### ⚠️ (d) Failure mode to probe first

MolmoAct2 conditions a flow-matching action expert on language-visual KV caches from a 2.3B VLM — rich, high-dimensional multi-modal token representations. D8 conditions the hand expert on body-expert action output a_b, a 7-DoF joint-space vector. The conditioning signal dimensionality and modality differ by orders of magnitude. Cheapest sanity check at CP1: ablate single-point FiLM(a_b) vs. 3-layer cross-attention(a_b) on the hand head in sim in-hand rotation, measuring cosine similarity of resulting hand trajectories across ablations, before committing to a full Transformer hand-head redesign.

---

## 🥈 Paper 2 — PRIORITY ★★

**HiVLA: A Visual-Grounded-Centric Hierarchical Embodied Manipulation System**
[arXiv:2604.14125](https://arxiv.org/abs/2604.14125) · Tianshuo Yang, Guanyu Chen, Yutong Chen, Zhixuan Liang, Yitian Liu, Zanxin Chen, Chunpu Xu, Haotian Liang, et al. · submitted: April 15, 2026 · source: keyword-sweep

### 🎯 (a) P# / D# touched

P1 — D7 (flow matching coordination), D8 (sharing module mechanism).

D7: HiVLA implements a strict two-level hierarchy — a VLM planner produces (subtask instruction + target bounding box) at low frequency, then a flow-matching Diffusion Transformer (DiT) action expert executes at high frequency. This is the most concrete published prior-art implementation of D7.v1 (body completes planning → output → lower expert denoises conditioned on that output) applied to a planning-vs-execution split rather than an arm-vs-hand split.

D8: HiVLA's DiT action expert is conditioned via cascaded cross-attention over 3 ordered streams: (1) global context tokens, (2) high-resolution object-centric crop tokens, (3) skill-semantic tokens from the VLM planner. This is a concrete (β) multi-layer + multi-stream variant of D8 option (C) cross-attention, empirically validated on real-robot fine-grained manipulation of small objects in cluttered scenes.

### ✨ (b) What is genuinely new

Cascaded cross-attention within a DiT action expert that serially integrates conditioning in fixed order (global → object-centric → skill-semantic) — unlike π0/π0.5's flat cross-attention over all VLM tokens at once, the ordered decomposition isolates what each attention pass attends to, outperforms flat cross-attention baselines on out-of-distribution fine-grained tasks in both simulation and real-world experiments, and preserves the VLM's zero-shot reasoning by decoupling planning entirely from low-level execution.

### ⚙️ (c) Decision implication

HiVLA's 3-stream cascaded cross-attention provides a concrete template for extending the D8 sharing module if the hand head is redesigned as a DiT before CP1. In `hand_head_cfg.py`, if switching to transformer-style hand head, set `hand_conditioning_streams = ["a_b", "hand_crop_vision"]` as a 2-stream cascaded cross-attention: body action a_b conditions first (coarser body intent), then a 64×64 hand-region visual crop conditions second (finer local state). The ordering follows HiVLA's coarse-to-fine principle and avoids the flat-pool cross-attention limitation. Metric to watch: per-finger contact precision on in-hand rotation vs. single-stream FiLM(a_b) baseline.

### ⚠️ (d) Failure mode to probe first

HiVLA's object-centric crop benefits manipulation of small objects seen in the wrist/global camera. The P1 hand expert needs contact-level information — tactile signals or finger proprioception — not a camera crop of the object. The cascade benefit may not appear until high-resolution hand-mounted cameras or tactile sensor tokens are available (CP2+). Cheapest check before CP1: run 2-stream cross-attention (a_b + hand visual crop) vs. single-stream FiLM(a_b) in sim in-hand rotation and measure whether the crop token adds any signal over noise in the hand trajectory.

---

## 📊 Scoring Summary

| # | Paper | Link | Relevance (0–3) | Novelty (0–3) | Reproducibility (0–3) | Sim2Real (0–3) | Total (/12) |
|---|-------|------|:---:|:---:|:---:|:---:|:---:|
| 1 | MolmoAct2 | [arXiv:2605.02881](https://arxiv.org/abs/2605.02881) | 3 | 2 | 3 | 2 | 10 |
| 2 | HiVLA | [arXiv:2604.14125](https://arxiv.org/abs/2604.14125) | 2 | 2 | 2 | 2 | 8 |

Only 2 papers passed the every-dimension ≥ 2 threshold. No 3rd paper is included.

---

## 🚫 Candidate Papers That Did Not Pass Filter

| Paper | Link | Reason dropped |
|-------|------|----------------|
| SnapFlow: One-Step Action Generation for Flow-Matching VLAs via Progressive Self-Distillation | [arXiv:2604.05656](https://arxiv.org/abs/2604.05656) | Sim2Real = 1: all primary experiments on LIBERO simulation suites and SmolVLA simulation; no real-robot validation of 1-NFE distillation. (Relevance D7 = 2, Novelty = 2, Reproducibility = 3.) |
| HiMoE-VLA: Hierarchical Mixture-of-Experts for Generalist VLA Policies | [arXiv:2512.05693](https://arxiv.org/abs/2512.05693) | Anti-topic: hierarchical router-based MoE for heterogeneous embodiments (different pattern from anatomical arm-hand split); no single-expert monolithic comparison baseline. |
| AdaMoE: Action-Specialized Mixture of Experts for VLA Learning | [arXiv:2510.14300](https://arxiv.org/abs/2510.14300) | Anti-topic: sparse router-based MoE expanding a pretrained VLA's feedforward layers; no anatomical arm-hand split; no dexterous-hand or tactile component. |
| UniDex: A Robot Foundation Suite for Universal Dexterous Hand Control | [arXiv:2603.22264](https://arxiv.org/abs/2603.22264) | Anti-topic: monolithic VLA without arm-hand split; FAAS (Function-Actuator-Aligned Space) is a cross-embodiment action space, not anatomical body/hand decoder partition. |
| Towards Robotic Dexterous Hand Intelligence: A Survey | [arXiv:2605.13925](https://arxiv.org/abs/2605.13925) | Novelty = 0, Reproducibility = 0, Sim2Real = 0: survey paper with no original experiments or implementations. |

---

## 💡 Context Suggestions

### 📌 Tracked literature

MolmoAct2 ([arXiv:2605.02881](https://arxiv.org/abs/2605.02881)) is a candidate for §6 Methodology Base (non-pinned) under P1 as the primary reference for D8 option (C)+(β) — per-layer cross-attention conditioning of a flow-matching action expert on a VLM backbone. The existing non-pinned table covers FiLM (Perez 2018) and PCGrad (Yu 2020) but has no reference for per-layer cross-attention conditioning. Suggested addition:

| MolmoAct2 (Fang et al., AllenAI / UW, 2026) | [arXiv:2605.02881](https://arxiv.org/abs/2605.02881) | Per-layer KV-cache conditioning of flow-matching action expert on VLM; concrete D8 option (C)+(β) reference at scale |

### 📌 Decision Log

D8 deferred (C) cross-attention: current trigger is "FiLM bottleneck OR hand head restructured to transformer / CP1." MolmoAct2 provides the first published large-scale empirical demonstration that per-layer cross-attention conditioning of a flow-matching expert on a VLM yields consistent improvement versus simpler conditioning baselines across multiple embodiments and task suites. The evidence is indirect (VLM→action-expert axis, not body-expert→hand-expert axis) but strengthens the prior for the CP1 architectural decision. Suggested annotation to D8 deferred (C): add "supporting empirical evidence: MolmoAct2 (2605.02881) — per-layer conditioning outperforms flat pooling in VLM→action-expert setting."

SnapFlow ([arXiv:2604.05656](https://arxiv.org/abs/2604.05656)): monitor for D7 at CP3. If real-robot follow-up validates 1-NFE consistency distillation on a physical platform, the body-expert denoising step count (`num_body_denoising_steps` in `pi0_train_cfg.py`) could be reduced from 10 to 1 via post-training consistency distillation, freeing ~60ms within the CP3 latency budget. No log change now.

### 📌 Anti-topics

No new anti-topic candidates. The existing 4-rule filter correctly excluded 5 papers this run. Filter health is confirmed.

### 📌 Researchers to follow

No new additions. Observation: MolmoAct2's primary authors (Haoquan Fang, Jiafei Duan — AllenAI / UW) are actively publishing open VLA architectures with novel conditioning mechanisms. Not a P1 primary group, but relevant to D8 conditioning-mechanism tracking. No §7 change warranted; note informally.

### 📌 Competitor / Kindred monitoring

No new releases from the §8 watch list in this window. DexReMoE, LaMP, and HEX had no new submissions found via WebSearch for May 15–17, 2026. Differentiation hypothesis (anatomical split + contact-grounded supervision wins on contact-precision; routing/motion-prior approaches win on generalization) remains untested and intact.

---

## 🔄 Day-over-Day Synthesis

- Papers already covered: none (first daily report in `research_log/`; only `_TEMPLATE.md` present).
- Contradictions with prior findings: none (no prior reports).
- Decision-Log triggers observed this run: none triggered. MolmoAct2 adds supporting evidence for the CP1 MLP vs. Transformer hand-head decision (D8) but does not trigger the deferred candidate — the trigger condition ("hand head restructured to transformer") has not been met yet.
- Anti-topic filter health: 5 papers excluded (2 router-based MoE, 1 monolithic VLA, 1 survey, 1 sim-only). Filter is calibrated correctly.
- Tool failure impact: Author Watch could not be executed (`semantic-scholar.get_author_papers` absent). Date-filtered Keyword Sweep could not be executed (`arxiv.search_papers` absent; arxiv.org and HF daily-papers pages returned HTTP 403; `mcp__Hugging_Face__hf_hub_query` for daily papers returned `{"result":[]}`). Both surfaced papers are outside the strict May 15–17, 2026 window (MolmoAct2: May 4; HiVLA: April 15). The report reflects the most recent relevant papers reachable with available tools, not a guaranteed complete scan of the 2-day window.
