---
presentation_of: 2607.26055
title: "πR²: Reactive Real-time Flow Policies"
venue: Preprint · Carnegie Mellon University · 2026
audience: 랩 그룹 미팅 · action chunking·flow policy 에 익숙한 로보틱스 연구자
minutes: 14
spine: 지연을 견디는 문제가 아니라, / 지연을 구조적으로 없애는 문제로 다시 쓴다
arxiv_html: 2607.26055v1
generated: 2026-09-09 01:10
generator: presentation/v1
---

## [起 · cover] 관절은 눈보다 천 배 빠르다

```probe-figure
{"id": "A1.F7", "url": "https://arxiv.org/html/2607.26055v1/fig/workspace.png",
 "caption": "오늘 이야기가 전부 벌어지는 자리 — 머리 위 카메라 한 대, 그리고 카메라보다 먼저 아는 손끝",
 "source": "Figure 7"}
```

- 무거운 백본을 그대로 둔 채 **25 Hz 폐루프** 힘 반응을 얻는 법
- 바꾼 것은 **AdaLN 한 줄**

```probe-script
오늘 설득하려는 문장은 하나입니다 — **모든 입력이 같은 속도로 처리될 필요는 없다.**

"VLA 를 빠르게 만드는 방법"으로 들으면 절반만 전달된 겁니다. 이 논문은 속도를 올린 게 아니라
**무엇을 언제 갱신할지를 다시 나눴습니다.**

넉 장으로 갑니다. 지금 무엇이 버려지고 있는지, 뻔한 해법이 왜 막히는지, 이 논문이 무엇을
쪼갰는지, 그리고 그 쪼갬이 실제로 손끝에서 보이는지.
```

## [起 · evidence] 청크를 던져놓고 눈을 감는다

```probe-figure
{"id": "S1.F1", "url": "https://arxiv.org/html/2607.26055v1/fig/concept_v5.png",
 "caption": "행동 청크가 실행되는 동안 들어오는 감각은 반영될 곳이 없습니다. 예측된 행동은 이미 낡은 관찰의 함수입니다.",
 "source": "Figure 1, 원문 §1"}
```

**실행 중에 들어온 감각은 버려집니다.** 접촉이 어긋나도 청크가 끝날 때까지 그대로 갑니다.

```probe-facts
[
 {
  "값": "45 차원",
  "라벨": "고유수용감각 — 그중 27 차원이 힘·토크"
 },
 {
  "값": "RGB 1 대",
  "라벨": "시각은 머리 위 640×480 한 장이 전부"
 },
 {
  "값": "25 Hz",
  "라벨": "실기 시연 수집 레이트 (원격조작 상한)"
 }
]
```

```probe-script
지금 VLA 의 표준 설계 셋 — 큰 VLM 백본, flow matching 정책, action chunking — 은 각각
도입될 때 분명한 이득이 있었습니다. 청구서는 **셋이 합쳐지고 나서** 도착했어요.

여기서 청중이 스스로 답을 예상하게 두세요. "그럼 청크를 짧게 잘라서 자주 다시 예측하면
되잖아?" — 다음 장이 그 예상을 깹니다. 그리고 깨지는 이유가 **품질이 아니라 계산**이라는 게
이 논문의 출발점입니다.
```

## [承 · budget] 그럼 매 tick 다시 예측하면 되지 않나

```probe-budget
{"이름": "호출 1 회",
 "제어주기": {"label": "50 Hz 제어 눈금", "ms": 20},
 "항목": [{"label": "이미지 전처리 + VLM", "ms": 60,
           "note": "보는 일 — 관찰 한 장을 토큰으로"},
          {"label": "DiT denoising (K=4 NFE)", "ms": 80,
           "note": "만드는 일 — NFE 4 회의 denoising"}],
 "출처": "원문 §1, RTX A6000 + GR00T-N1.7"}
```

호출 하나가 **140 ms**. 50 Hz 제어에서 **7 tick** 입니다. 이 140 ms 를 어떻게 쪼개느냐가 논문 전체입니다.

```probe-facts
[
 {
  "값": "20 ms",
  "라벨": "50 Hz 제어 눈금 하나"
 },
 {
  "값": "16 스텝",
  "라벨": "표준 flow 가 청크 하나에 쓰는 denoising"
 },
 {
  "값": "1 스텝",
  "라벨": "πR² 가 호출당 쓰는 denoising"
 }
]
```

```probe-script
막대를 손으로 짚으면서 말하세요. 막대를 가르는 흰 선이 제어 눈금입니다. **한 칸이 20 ms**,
그리고 이 막대는 일곱 칸입니다.

순진한 해법이 막히는 지점이 여기서 눈으로 보입니다. 매 tick 다시 예측하려면 이 막대 전체를
매 tick 통과해야 하는데, 눈금 하나 안에 끝내야 할 일이 일곱 칸을 잡아먹습니다.

그러니 질문이 "청크를 얼마나 짧게 자를까"가 아니라 **"이 막대에서 무엇을 빼낼 수 있을까"**로
바뀝니다. 두 토막이 서로 다른 성질이라는 게 다음 장의 실마리입니다.
```

## [轉 · statement] 모든 입력이 같은 속도로 / 처리될 필요는 없다

고유수용감각은 이미지·언어보다 **수 자릿수 빠르게** 조회됩니다. / 그리고 동적 과제에서 국소 반응 교정에는 그것으로 충분합니다.

```probe-diagram
{"before": {"label": "지금",
  "chain": [{"box": "비전 · 언어 · 고유수용감각", "note": "한 덩어리로 들어간다"},
            {"box": "백본 전체 통과", "note": "140 ms"},
            {"box": "행동"}]},
 "after": {"label": "πR²",
  "slow": {"box": "비전 · 언어", "note": "비동기 · 캐시해서 쓴다"},
  "fast": {"box": "고유수용감각", "note": "매 tick 갱신", "hot": true},
  "join": {"box": "액션 헤드", "note": "1 NFE · 20 ms", "hot": true},
  "out": {"box": "행동"}},
 "why": "원문 Figure 1 은 노이즈 스케줄까지 함께 담은 전체 개념도라, 채널이 갈라지는 이 한 장면만 떼어낸 그림은 논문에 없습니다."}
```

```probe-facts
[
 {
  "값": "27 / 45",
  "라벨": "고유수용 차원 중 힘·토크가 차지하는 몫 — 국소 교정에 충분한 이유"
 },
 {
  "값": "0 – 5 tick",
  "라벨": "학습 때 시각에 일부러 주는 지연, 균등 분포 — 25 Hz 에서 최대 200 ms"
 },
 {
  "값": "AdaLN 1 줄",
  "라벨": "청크 위치마다 (γ, β) 를 갖게 하는 변경 — 1 NFE 를 가능하게 하는 계단 스케줄"
 }
]
```

```probe-script
여기서 잠깐 멈추세요. 이게 이 논문의 전환입니다.

지금까지 "관찰"은 한 덩어리였습니다. 카메라도 관절 센서도 다 같이 들어가서 다 같이 처리됐어요.
이 논문은 그걸 **처리 속도로 갈라칩니다.** 그리고 그 갈라침에는 근거가 있습니다 — 시각·언어는
**거친 운동 계획**을 이끄는 전역 맥락을 주고, 고유수용감각은 **국소 반응 교정**에 충분한 정보를
담습니다. 역할이 원래부터 달랐던 겁니다.

그림의 위쪽 갈래부터. 낡음을 그냥 견디는 게 아니라 **학습시킵니다.** 학습 때 시각을 일부러
0~5 tick 늦추고, 그 정수 지연값을 임베딩으로 만들어 더해 줍니다. 25 Hz 에서 5 tick 이면 200 ms —
모델이 "지금 내 눈이 0.2 초 낡았다"를 **알고** 행동한다는 뜻이에요.

아래쪽 갈래. 고유수용감각만 매 tick 갱신해서 액션 헤드에 바로 넣습니다. 그리고 denoising 을
호출당 한 스텝으로 줄이는데, 이게 그냥 줄이는 게 아니라 청크 위치마다 다른 노이즈 레벨을 주는
**계단 스케줄** 덕분입니다. 앞은 못 박고, 가운데는 비탈, 뒤는 순수 노이즈 — 그래서 매 호출이
한 스텝씩만 밀어도 앞쪽부터 완성돼 나옵니다. 결과적으로 호출당 비용이 **백본 크기와 무관**해져요.

앞 장의 막대를 다시 떠올리게 하세요. 60 ms 쪽과 80 ms 쪽이 각각 다른 장치로 빠진 겁니다.

질문 나오면: RTC 도 앞부분을 못 박지만 나머지에 **하나의 공유 노이즈 레벨**을 씁니다. 계단은
그걸 램프로 바꿔서 단일 스텝 방출을 가능하게 한 겁니다.
```

## [轉 · timing] 두 장치가 각각 무엇을 덜어내는가

```probe-timing
{"제어주기": {"label": "25 Hz tick", "ms": 40}, "span_ms": 160,
 "줄": [
  {"name": "기준선 (naive-async · RTC)", "blocks": [
     {"label": "VLM 60", "at": 0, "ms": 60, "kind": "wait"},
     {"label": "denoising 4 NFE 80", "at": 60, "ms": 80, "kind": "wait"}],
   "mark": {"at": 140, "label": "유효 지연 140 ms = 1.75 d₀"}},
  {"name": "denoising 만 줄이면", "blocks": [
     {"label": "VLM 60", "at": 0, "ms": 60, "kind": "wait"},
     {"label": "20", "at": 60, "ms": 20, "kind": "run"}],
   "mark": {"at": 80, "label": "80 ms = 1.0 d₀"}},
  {"name": "πR² — 백본까지 빼내면", "blocks": [
     {"label": "1 NFE", "at": 0, "ms": 20, "kind": "run"}],
   "mark": {"at": 20, "label": "20 ms = 0.25 d₀", "us": true}}]}
```

```probe-facts
[
 {
  "값": "d = 4 – 5",
  "라벨": "기준선 실기 지연, 25 Hz tick 단위 (RTX A5000)"
 },
 {
  "값": "d = 1 – 2",
  "라벨": "πR² 실기 지연, 같은 하드웨어"
 },
 {
  "값": "40 ms",
  "라벨": "25 Hz tick 하나 — 마지막 줄이 한 칸에 들어온다"
 }
]
```

```probe-script
이 그림이 이 발표의 심장입니다. **두 장치가 각각 얼마를 덜어내는지**가 줄마다 보입니다.

첫째 줄이 지금까지의 모든 방법입니다 — naive-async 도, 가장 강한 baseline 인 train-time RTC 도
유효 지연이 똑같이 1.75 d₀ 예요. 둘 다 **호출마다 백본 전체를 통과**하기 때문입니다.

둘째 줄은 계단 스케줄만 적용한 것. denoising 이 4 스텝에서 1 스텝으로 줄어 80 ms 가 빠집니다.

셋째 줄이 백본까지 비동기로 빼낸 πR² 전체입니다. 호출 경로에 남는 건 1 NFE, 20 ms 뿐이에요.
25 Hz tick 하나가 40 ms 니까 **한 칸 안에 들어옵니다.** 그래서 폐루프가 성립합니다.

여기가 논문 프레임이 바뀌는 자리이기도 합니다 — 앞의 둘은 지연을 **견디는** 방법이고,
이건 지연을 **없애는** 방법입니다.
```

## [結 · evidence] 손끝이 50 N 에서 멈춘다

```probe-figure
{"id": "S4.F5", "url": "https://arxiv.org/html/2607.26055v1/fig/proprio_book.png",
 "caption": "Tidy Up Book. 지문 힘(실선, 좌축)과 방출된 행동(점선, 우축). πR² 는 힘 피드백에 반응해 약 50 N 에서 멈추고, baseline 은 낡은 계획을 그대로 실행해 120 N 까지 올라갑니다.",
 "source": "Figure 5, 원문 §4"}
```

같은 백본, 같은 데이터. 다른 것은 **실행 중에 손끝을 보느냐** 뿐입니다.

```probe-facts
[
 {
  "값": "약 50 N",
  "라벨": "πR² — 힘 피드백으로 쥐는 힘을 조절하고 멈춘다"
 },
 {
  "값": "120 N",
  "라벨": "baseline — 낡은 계획을 그대로 실행"
 },
 {
  "값": "xArm6 + XHand",
  "라벨": "GR00T-N1.7 파인튜닝, 같은 데이터"
 }
]
```

```probe-script
이 그림 한 장이 앞의 모든 이야기를 손에 잡히게 만듭니다. 숫자가 아니라 **곡선의 모양**을 보세요.

baseline 은 힘이 올라가는 동안 행동 곡선이 반응하지 않습니다. 계획이 이미 정해졌으니까요.
πR² 는 힘이 올라가자 행동이 꺾입니다. 50 N 과 120 N 의 차이는 정책 품질이 아니라
**감각이 도달했느냐**의 차이예요.

숫자 하나 덧붙이면 좋습니다: 이 시스템의 고유수용감각은 45 차원인데 그중 **27 차원이 힘·토크**
입니다. 시각은 머리 위 RGB 한 장이 전부고요. 차원 수로 보면 손끝이 조작을 이끕니다.
```

## [結 · lineage] 지연을 견디는 문제에서, 없애는 문제로

```probe-lineage
{"items": [
  {"when": "2025-06", "what": "RTC", "gave": "추론 시점 알고리즘만으로 비동기 실행 — 재학습 불필요"},
  {"when": "2025-12", "what": "Train-Time RTC", "gave": "그 오버헤드를 학습 시점으로 — 가장 강한 baseline"}],
 "me": {"when": "2026-07", "what": "πR²",
        "gave": "백본을 비동기로 빼내고 denoising 을 호출들에 나눠 담는다"}}
```

앞의 둘은 여전히 **호출마다 백본 전체를 통과**합니다.

```probe-facts
[
 {
  "값": "1.75 d₀",
  "라벨": "앞의 둘은 유효 지연이 서로 같다 — 백본을 못 뺐다"
 },
 {
  "값": "0.25 d₀",
  "라벨": "πR² — 호출 경로에 1 NFE 만 남는다"
 }
]
```

```probe-script
계보를 짚는 이유는 이 논문의 기여를 정확히 자르기 위해서입니다.

RTC 계열은 "지연이 있다는 걸 인정하고 그 위에서 잘 잇자"는 접근이었습니다. 진행 중인 행동을
앵커로 삼아 다음 청크를 매끄럽게 붙이는 거죠. 그건 지연을 **견디는** 겁니다.

πR² 는 지연이 어디서 오는지를 다시 보고 **호출 경로에서 빼냅니다.** 같은 문제에 대한 답이
아니라, 문제를 다시 쓴 겁니다.

그리고 이건 우리 코퍼스이기 때문에 그릴 수 있는 그림입니다 — 저 두 편의 재작성본이 이미
여기 있으니까요.
```

## [結 · ledger] 무엇을 사고 무엇을 파는가

```probe-ledger
{"buy": {"head": "산다",
 "lines": [
   {"t": "호출당 비용이 **백본 크기와 무관**",
    "n": "1 NFE 고정 · 20 ms — 25 Hz tick 한 칸(40 ms) 안"},
   {"t": "**AdaLN 한 줄**만 바꾸면 어떤 DiT flow 정책에도 꽂힌다",
    "n": "위치별 (γ_p, β_p) · 사전학습 공유 쌍에서 초기화 · 백본 무변경"},
   {"t": "백본을 그대로 두고 **25 Hz 폐루프**를 얻는다",
    "n": "GR00T N1.7 · xArm6 + XHand · RTX A5000"}],
 "foot": "사는 자리는 **국소 교정**이다"},
 "sell": {"head": "판다",
 "lines": [
   {"t": "시각이 **최대 200 ms 낡은** 채로 동작한다",
    "n": "학습이 견디게 만든 상한(5 tick)이지, 재어 본 한계가 아니다"},
   {"t": "진행 중 행동을 앵커로 쓰는 성질이 **방향을 바꿔야 할 때 족쇄**가 된다",
    "n": "논문 스스로 Insert Box 에서 보고하는 실패 모드"},
   {"t": "실기 시연이 **25 Hz 상한**",
    "n": "관절 서보는 보통 500 Hz — 그 사이 레이트는 미검증"}],
 "foot": "파는 자리는 **전역 재계획**이다"}}
```

```probe-facts
[
 {
  "값": "1 줄",
  "라벨": "바꾼 코드 — AdaLN 조건화를 위치별로"
 },
 {
  "값": "0.25 d₀",
  "라벨": "남은 유효 지연 — 기준선 1.75 d₀ 대비"
 },
 {
  "값": "200 ms",
  "라벨": "감수하기로 한 시각 낡음 — 이 거래의 값"
 }
]
```

```probe-script
이 장을 건너뛰지 마세요. 여기가 정직한 자리입니다.

가장 실질적인 건 두 번째 항목입니다. 진행 중인 행동을 앵커로 삼는 바로 그 메커니즘이,
계획을 **뒤집어야** 하는 순간에는 발목을 잡습니다. 논문이 Insert Box 에서 그걸 직접 보고합니다.

칸 아래 두 줄이 이 장의 요약입니다 — 사는 건 **국소 교정**, 파는 건 **전역 재계획**. 우리 과제가
어느 쪽인지에 따라 값이 완전히 달라집니다.
```

## [結 · ledger] 증명된 것과 증명되지 않은 것

```probe-ledger
{"buy": {"head": "증명됐다",
 "lines": [
   {"t": "관찰을 **처리 속도로 쪼개면** 유효 지연이 1.75 d₀ → 0.25 d₀",
    "n": "같은 하드웨어 실측 d = 4–5 tick → 1–2 tick"},
   {"t": "가장 강한 baseline(train-time RTC)도 **백본 통과 비용은 못 줄인다**",
    "n": "naive-async 와 유효 지연이 똑같은 1.75 d₀ 로 겹친다"},
   {"t": "손끝 힘 반응이 **실기에서 곡선으로** 나타난다",
    "n": "Figure 5 — 약 50 N 에서 정지, baseline 은 120 N 까지"}],
 "foot": "**쪼갠다**는 주장 자체는 섰다"},
 "sell": {"head": "아직이다",
 "lines": [
   {"t": "시뮬레이션은 **손 과제 하나** — 큐브 재배향",
    "n": "MuJoCo Playground · Leap Hand 16-DoF · 실기와 다른 몸"},
   {"t": "지연 임베딩은 실기에만 쓰였다 — **필수가 아니라 보강**",
    "n": "부록 A.1 — 시뮬레이션에서는 아예 쓰지 않는다"},
   {"t": "느린 채널이 얼마나 낡아도 되는지의 **상한은 어디인가**",
    "n": "200 ms 는 학습시킨 값이지 측정된 한계가 아니다"}],
 "foot": "우리 과제에서 **다시 재야 하는 것**"}}
```

```probe-facts
[
 {
  "값": "1 / 1",
  "라벨": "실기 로봇 한 대 · 시뮬레이션 과제 하나 — 증거의 폭"
 },
 {
  "값": "0 개",
  "라벨": "시뮬레이션에서 쓰인 지연 임베딩 — 떼어낼 수 있는 조각"
 },
 {
  "값": "5 tick",
  "라벨": "학습이 견디게 만든 상한 — 그 위는 아무도 재지 않았다"
 }
]
```

```probe-script
마무리는 증명된 것과 아닌 것을 가르는 걸로. 발표에서 논문을 파는 게 아니라 **읽을 값어치를
판정해 주는 게** 우리 미팅의 목적이니까요.

가운데 항목을 짚어 주세요. 학습된 지연 임베딩은 시뮬레이션에서는 **아예 쓰이지 않았습니다.**
논문 스스로 부록에서 그렇게 적어요. 이식할 때 우선순위를 낮게 둘 수 있는 부품이라는 뜻이고,
우리가 가져다 쓸 때 **먼저 뗄 수 있는 조각**이기도 합니다.

마지막 질문이 우리 팀에 가장 실질적입니다 — 200 ms 는 이 논문이 학습시킨 상한일 뿐이지
측정된 한계가 아닙니다. 우리 과제에서 시각이 얼마나 낡아도 되는지는 다시 재야 합니다.
```
